
&Вместо("НалогообложениеНДС")
Функция РасшAzRy_НалогообложениеНДС(Организация, Склад, Дата, Контрагент, Договор)
	
	//Паганини_
	//Запрос = Новый Запрос;
	//
	//Если ЗначениеЗаполнено(Дата) Тогда
	//	Запрос.УстановитьПараметр("Дата",Дата);
	//Иначе
	//	Запрос.УстановитьПараметр("Дата", ТекущаяДата());
	//КонецЕсли;
	//
	//Запрос.УстановитьПараметр("Организация", Организация);
	//Запрос.УстановитьПараметр("Склад", Склад);
	//
	//Запрос.Текст = 
	//"ВЫБРАТЬ
	//|	ЗНАЧЕНИЕ(Перечисление.ТипыНалогообложенияНДС.НеОблагаетсяНДС) КАК НалогообложениеНДС
	//|ИЗ
	//|	РегистрСведений.ПримененияЕНВД.СрезПоследних(
	//|			&Дата,
	//|			Организация = &Организация
	//|				И СтруктурнаяЕдиница = &Склад) КАК ПримененияЕНВД
	//|ГДЕ
	//|	ПримененияЕНВД.РозничнаяТорговляОблагаетсяЕНВД
	//|
	//|ОБЪЕДИНИТЬ ВСЕ
	//|
	//|ВЫБРАТЬ
	//|	ЗНАЧЕНИЕ(Перечисление.ТипыНалогообложенияНДС.НеОблагаетсяНДС)
	//|ИЗ
	//|	РегистрСведений.СистемыНалогообложенияОрганизаций.СрезПоследних(&Дата, Организация = &Организация) КАК Налогообложение
	//|ГДЕ
	//|	Налогообложение.СистемаНалогообложения = ЗНАЧЕНИЕ(Перечисление.СистемыНалогообложения.Упрощенная)";
	//
	//НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС;
	//
	//Выборка = запрос.Выполнить().Выбрать();
	//Если Выборка.Следующий() Тогда
	//	НалогообложениеНДС = Выборка.НалогообложениеНДС;
	//КонецЕсли;

//	Если ЗначениеЗаполнено(Контрагент) Тогда
//		Если Организация.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС Тогда
//			Если НЕ Договор = Неопределено И Договор.УчитыватьНДФЛ Тогда
//				НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.НеОблагаетсяНДС;
//			Иначе
//				СтруктураСреза = РегистрыСведений.НалогообложениеКонтрагентов.ПолучитьПоследнее(Дата, Новый Структура("Контрагент,Договор", Контрагент, Договор));
//				Если НЕ ЗначениеЗаполнено(СтруктураСреза.НалогообложениеНДС) Тогда 
//					НалогообложениеНДС = ?(ЗначениеЗаполнено(Контрагент.НалогообложениеНДС), Контрагент.НалогообложениеНДС, Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС);
//				Иначе
//					НалогообложениеНДС = СтруктураСреза.НалогообложениеНДС;
//				КонецЕсли;
//			КонецЕсли;
//		Иначе
//			НалогообложениеНДС = Организация.НалогообложениеНДС;
//		КонецЕсли;
//	Иначе
//		НалогообложениеНДС = ?(ЗначениеЗаполнено(Организация.НалогообложениеНДС), Организация.НалогообложениеНДС, Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС);
//	КонецЕсли;
	
	ДанныеОрганизации = Организация;
	Если ПолучитьФункциональнуюОпцию("ФункциональнаяОпцияИспользоватьИсториюНалогообложенияОрганизации") Тогда 
		ДанныеОрганизации = РегистрыСведений.НалогообложениеОрганизации.ПолучитьПоследнее(Дата, Новый Структура("Организация", Организация));
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Контрагент) Тогда
		Если ДанныеОрганизации.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС Тогда
			// +++ { AzRy - [28.01.2021] - Jira:ERPM-809 - Developer:გიორგი მამულაშვილი - Comment:
			Если НЕ Договор = Неопределено И Договор.УчитыватьНДФЛ И Не Контрагент.ЯвляетсяНеРезидентом Тогда
			// +++ } AzRy - [28.01.2021] - Jira:ERPM-809 - Developer:გიორგი მამულაშვილი - Comment:
				НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.НеОблагаетсяНДС;
			Иначе
				ДанныеКонтрагента = Контрагент;
				Если ПолучитьФункциональнуюОпцию("ФункциональнаяОпцияИспользоватьИсториюНалогообложенияКонтрагентов") Тогда  
					ДанныеКонтрагента = РегистрыСведений.НалогообложениеКонтрагентов.ПолучитьПоследнее(Дата, Новый Структура("Контрагент,Договор", Контрагент, Договор));
				КонецЕсли;
				
				Если НЕ ЗначениеЗаполнено(ДанныеКонтрагента.НалогообложениеНДС) 
					И Контрагент.ЯвляетсяНеРезидентом
					И Контрагент.НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.НаЭкспорт Тогда 
					
					НалогообложениеНДС = Перечисления.ТипыНалогообложенияНДС.НаЭкспорт;
					
				ИначеЕсли НЕ ЗначениеЗаполнено(ДанныеКонтрагента.НалогообложениеНДС) Тогда 
					НалогообложениеНДС = ?(ЗначениеЗаполнено(Контрагент.НалогообложениеНДС) , Контрагент.НалогообложениеНДС, Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС);
				Иначе
					НалогообложениеНДС = ДанныеКонтрагента.НалогообложениеНДС;
				КонецЕсли;
			КонецЕсли;
		Иначе
			НалогообложениеНДС = ДанныеОрганизации.НалогообложениеНДС;
		КонецЕсли;
	Иначе
		НалогообложениеНДС = ?(ЗначениеЗаполнено(ДанныеОрганизации.НалогообложениеНДС), ДанныеОрганизации.НалогообложениеНДС, Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС);
	КонецЕсли;

	//_Паганини
	
	Возврат НалогообложениеНДС;
	
КонецФункции
