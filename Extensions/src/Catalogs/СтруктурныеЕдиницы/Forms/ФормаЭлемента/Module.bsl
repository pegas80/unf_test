
&НаСервере
Процедура РасшAzRy_ПриСозданииНаСервереПосле(Отказ, СтандартнаяОбработка)
	
	Если Объект.ТипСтруктурнойЕдиницы = Перечисления.ТипыСтруктурныхЕдиниц.Склад Тогда
	    Элементы.AzRyТекущийОтветственный.Видимость = Истина;
	Иначе
	    Элементы.AzRyТекущийОтветственный.Видимость = Ложь;
	КонецЕсли;
	
	Если Не РольДоступна("AzRyНапоминанияПользователяАдминистратор") Тогда
		Элементы.AzRyТекущийОтветственный.ТолькоПросмотр = Истина;
	КонецЕсли; 
	
КонецПроцедуры
