
&Вместо("ПередЗаписью")
Процедура РасшAzRy_ПередЗаписью(Отказ)
	////Паганини_
	//Если Не ЭтоГруппа Тогда
	//	Если НЕ ПометкаУдаления И НЕ РС_ОбщегоНазначения.РеквизитУникален(Ссылка, "ИНН", ИНН, "ФизическиеЛица") Тогда
	//		ТекстСообщения = НСтр("ka='ს/ნ არ არის უნიკალური!';ru='ИНН не уникален!';en='VAT is not unique!'");
	//		УправлениеНебольшойФирмойСервер.СообщитьОбОшибке(ЭтотОбъект, ТекстСообщения, , , "ИНН", Отказ);
	//	КонецЕсли;
	//КонецЕсли;
	////_Паганини
КонецПроцедуры
