
&НаКлиенте
Процедура ЗагрузитьКурсыВалют(Команда)
	СчитатьКурсыИЗагрузить(ПутьКRSS, РежимВыводаСообщений)	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ПутьКRSS = "https://nbg.gov.ge/gw/api/ct/monetarypolicy/currencies/ka/json";//"http://www.nbg.gov.ge/rss.php";
	ТекущаяДата = ТекущаяДата();
	РежимВыводаСообщений = Ложь;
КонецПроцедуры

&НаСервере
Функция СчитатьКурсыИЗагрузить(ПутьКRSS, РежимВыводаСообщений)
	ОбъектОбработка = РеквизитФормыВЗначение("Объект");
	//ДатаИзДанных = ОбъектОбработка.ЗагрузитьКурсыВалют(ПутьКRSS, РежимВыводаСообщений);
	ДатаИзДанных = ОбъектОбработка.ЗагрузитьКурсыВалютSSl(ПутьКRSS, РежимВыводаСообщений);  //Lazare 25.08.2022
    ЗначениеВРеквизитФормы(ОбъектОбработка, "Объект");
	
	
КонецФункции

&НаКлиенте
Процедура ЗагрузитьИсториюКурсов(Команда)
	ОткрытьФорму("Обработка.ЗагрузкаКурсовВалютГрузия.Форма.ФормаИсторияКурсов");
КонецПроцедуры

&НаКлиенте
Процедура РежимОтладки(Команда)
	Объект.РежимОтладки = не Объект.РежимОтладки;
	УправлениеВидимостью();
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	УправлениеВидимостью();
КонецПроцедуры

&НаКлиенте
Процедура УправлениеВидимостью()
	Элементы.ТекстОтвета.Видимость = Объект.РежимОтладки;
КонецПРоцедуры