
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Таблица = ПолучитьИзВременногоХранилища(Параметры.АдресТаблицы);
	ТаблицаАвансов.Загрузить(Таблица);
КонецПроцедуры

&НаКлиенте
Процедура OK(Команда)
	Закрыть(Новый Структура("АдресТаблицы", ПолучитьВыделенныеСтроки()));
КонецПроцедуры

&НаСервере
Функция ПолучитьВыделенныеСтроки()
	Возврат ПоместитьВоВременноеХранилище(ТаблицаАвансов.Выгрузить(Новый Структура("Флаг", Истина)));
КонецФункции