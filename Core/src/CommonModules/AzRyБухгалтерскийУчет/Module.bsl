///////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ РАБОТЫ СО СЧЕТАМИ И СУБКОНТО

// Процедура устанавливает субконто на счете. Если такое субконто на счете
// отсутствует, то ничего не делается.
//
// Параметры:
//	Счет         - Счет, к которому относится субконто
//  Субконто     - набор субконто
//	ИмяСубконто  - Номер, имя или вид устанавливаемого субконто
//  Значение субконто - значение устанавливаемого субконто
//
Процедура УстановитьСубконто(Счет, ЗначениеСубконто, Проводка, Выборка, ДтКт) Экспорт

	//Если НЕ ЗначениеЗаполнено(ИмяСубконто) Тогда
	//	Возврат;
	//КонецЕсли;
	
	СвойстваСчета = БухгалтерскийУчетВызовСервераПовтИсп.ПолучитьСвойстваСчета(Счет);
	
	//Если ТипЗнч(ИмяСубконто) = Тип("Число") Тогда // номер субконто

	//	Если ИмяСубконто > СвойстваСчета.КоличествоСубконто Тогда
	//		Возврат;
	//	КонецЕсли;

	//	ВидСубконто = СвойстваСчета["ВидСубконто" + ИмяСубконто];

	//Иначе // имя или вид субконто

	//	ВидСубконто = ?(ТипЗнч(ИмяСубконто) = Тип("ПланВидовХарактеристикСсылка.ВидыСубконтоХозрасчетные"),
	//					ИмяСубконто,
	//					ПланыВидовХарактеристик.ВидыСубконтоХозрасчетные[ИмяСубконто]);
	//	
	//	// Проверка, что указанный вид субконто есть на счете.
	//	Если СвойстваСчета.ВидСубконто1 <> ВидСубконто
	//	   И СвойстваСчета.ВидСубконто2 <> ВидСубконто
	//	   И СвойстваСчета.ВидСубконто3 <> ВидСубконто Тогда

	//		Возврат;
	//		
	//	КонецЕсли;

	//КонецЕсли;
		
	//Если ВидСубконто.ТипЗначения.СодержитТип(ТипЗнч(ЗначениеСубконто)) Тогда
	//	
	//	Субконто.Вставить(ВидСубконто, ЗначениеСубконто);
	//	
	//КонецЕсли;
	                                                           
	Для каждого ТекСубконто Из Счет.ВидыСубконто Цикл
	    Если ТекСубконто.ВидСубконто.ИмяПредопределенныхДанных       = "Подразделения" И ЗначениеСубконто.Свойство("Подразделения") Тогда
			Проводка["Субконто" + ДтКт + ТекСубконто.НомерСтроки]    = ЗначениеСубконто.Подразделения;
			Проводка["ВидСубконто" + ДтКт + ТекСубконто.НомерСтроки] = СвойстваСчета["ВидСубконто" + ТекСубконто.НомерСтроки].ИмяПредопределенныхДанных;
		ИначеЕсли ТекСубконто.ВидСубконто.ИмяПредопределенныхДанных  = "Номенклатура" И ЗначениеСубконто.Свойство("Номенклатура") Тогда
			Проводка["Субконто" + ДтКт + ТекСубконто.НомерСтроки]    = ЗначениеСубконто.Номенклатура;
			Проводка["ВидСубконто" + ДтКт + ТекСубконто.НомерСтроки] = СвойстваСчета["ВидСубконто" + ТекСубконто.НомерСтроки].ИмяПредопределенныхДанных;
		ИначеЕсли ТекСубконто.ВидСубконто.ИмяПредопределенныхДанных  = "Контрагенты" И ЗначениеСубконто.Свойство("Контрагенты") Тогда
			Проводка["Субконто" + ДтКт + ТекСубконто.НомерСтроки]    = ЗначениеСубконто.Контрагенты;
			Проводка["ВидСубконто" + ДтКт + ТекСубконто.НомерСтроки] = СвойстваСчета["ВидСубконто" + ТекСубконто.НомерСтроки].ИмяПредопределенныхДанных;	
		ИначеЕсли ТекСубконто.ВидСубконто.ИмяПредопределенныхДанных  = "Договоры" И ЗначениеСубконто.Свойство("Договоры") Тогда
			Проводка["Субконто" + ДтКт + ТекСубконто.НомерСтроки]    = ЗначениеСубконто.Договоры;
			Проводка["ВидСубконто" + ДтКт + ТекСубконто.НомерСтроки] = СвойстваСчета["ВидСубконто" + ТекСубконто.НомерСтроки].ИмяПредопределенныхДанных;
		ИначеЕсли ТекСубконто.ВидСубконто.ИмяПредопределенныхДанных  = "КонтрагентыСотрудники" И ЗначениеСубконто.Свойство("КонтрагентыСотрудники") Тогда
			Проводка["Субконто" + ДтКт + ТекСубконто.НомерСтроки]    = ЗначениеСубконто.КонтрагентыСотрудники;
			Проводка["ВидСубконто" + ДтКт + ТекСубконто.НомерСтроки] = СвойстваСчета["ВидСубконто" + ТекСубконто.НомерСтроки].ИмяПредопределенныхДанных;			
		КонецЕсли; 
	КонецЦикла;

КонецПроцедуры // УстановитьСубконто()
