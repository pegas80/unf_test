// Функция возвращает ссылку на подчиненный счет-фактуру
//
Функция ПолучитьПодчиненныйСчетФактура(ДокументОснование) Экспорт

	Если Не ЗначениеЗаполнено(ДокументОснование) Тогда
		Возврат Неопределено;	
	КонецЕсли;
		
	// Для исходящих СФ документы основания хранятся в ТЧ ДокументыОснования
	ТекстЗапроса = 
	"ВЫБРАТЬ
	|	СчетФактураОтправленныйГрузияДокументыОснования.Ссылка,
	|	СчетФактураОтправленныйГрузияДокументыОснования.Ссылка.Номер,
	|	СчетФактураОтправленныйГрузияДокументыОснования.Ссылка.Дата
	|ИЗ
	|	Документ.СчетФактураОтправленныйГрузия.ДокументыОснования КАК СчетФактураОтправленныйГрузияДокументыОснования
	|ГДЕ
	|	СчетФактураОтправленныйГрузияДокументыОснования.ДокументОснование = &ДокументОснование";
	
	
	Запрос = Новый Запрос(ТекстЗапроса);
	Запрос.УстановитьПараметр("ДокументОснование", ДокументОснование);
	РезультатЗапроса = Запрос.Выполнить();
	
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	Выборка.Следующий();
	
	СтруктураВозврата = Новый Структура;
	СтруктураВозврата.Вставить("Ссылка", Выборка.Ссылка);
	СтруктураВозврата.Вставить("Номер", Выборка.Номер);
	СтруктураВозврата.Вставить("Дата", Выборка.Дата);
	
	Возврат СтруктураВозврата;

КонецФункции

Функция УчетнаяПолитика(ТекДата, Организация) Экспорт

	ТекУчетнаяПолитика = РегистрыСведений.УчетнаяПолитикаРегл.ПолучитьПоследнее(ТекДата, Новый Структура("Организация", Организация));	
	Возврат ТекУчетнаяПолитика;

КонецФункции
