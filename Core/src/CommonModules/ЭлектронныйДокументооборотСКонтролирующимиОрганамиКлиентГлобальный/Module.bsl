Процедура ПроверитьОтветыНаЗаявленияНаПодключениеКлиент() Экспорт
		ПроверитьОтветыНаЗаявленияНаПодключение();
		ПодключитьОбработчикОжидания("ПроверитьОтветыНаЗаявленияНаПодключение",86400); // = 24(ч)*60(мин)*60(сек) = 1 сутки
КонецПроцедуры

Процедура ПроверитьОтветыНаЗаявленияНаПодключение() Экспорт
	Попытка
		ОписаниеОповещения = Новый ОписаниеОповещения("ПроверитьОтветыНаЗаявленияНаПодключениеЗавершение", ЭлектронныйДокументооборотСКонтролирующимиОрганамиКлиент);
		ДокументооборотСКОКлиент.ПолучитьКонтекстЭДО(ОписаниеОповещения);
	Исключение
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(НСтр("ka='საინფორმაციო ურთიერთქმედებასთან შეერთების განცხადებებზე პასუხების არსებობის ავტომატური შემოწმების დამამუშავებლის ინიციალიზება ვერ მოხერხდა';ru='Не удалось инициализировать обработчик автоматической проверки наличия ответов на заявления подключения к информационному взаимодействию';en='Failed to initialize the handler automatically check for responses to the application to connect to Information Interaction'") + ":
			|" + ИнформацияОбОшибке().Описание);
	КонецПопытки;
КонецПроцедуры
