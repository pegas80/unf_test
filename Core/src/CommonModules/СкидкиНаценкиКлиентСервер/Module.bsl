
//Рассчитывает сумму и процент ручной скидки (без учета автоматической скидки) для документа
//Процент скидки может быть разным для разных строк, тогда общий процент ручной скидки рассчитывается от суммы документа
Процедура РассчитатьСуммуИПроцентСкидки(Форма, ИмяТабличнойЧасти = "Запасы", ВариантКП = Неопределено) Экспорт
	
	ОбщаяСуммаРучнойСкидки	= 0;
	ОбщаяСуммаБезСкидок		= 0;
	Для каждого стр Из Форма.Объект[ИмяТабличнойЧасти] Цикл
		
		Если ВариантКП <> Неопределено И стр.НомерВариантаКП <> ВариантКП Тогда
			Продолжить;
		КонецЕсли;
		
		ОбщаяСуммаБезСкидок = ОбщаяСуммаБезСкидок + стр.Цена * стр.Количество;
		Если стр.ПроцентСкидкиНаценки = 0 Тогда
			Продолжить;
		ИначеЕсли стр.Свойство("СуммаАвтоматическойСкидки") Тогда
			ОбщаяСуммаРучнойСкидки = ОбщаяСуммаРучнойСкидки + стр.Цена * стр.Количество - стр.Сумма - стр.СуммаАвтоматическойСкидки;
		Иначе
			ОбщаяСуммаРучнойСкидки = ОбщаяСуммаРучнойСкидки + стр.Цена * стр.Количество - стр.Сумма;
		КонецЕсли; 
		
	КонецЦикла;
	
	Форма.СкидкаСумма = ОбщаяСуммаРучнойСкидки;
	Форма.СкидкаПроцент = ?(ОбщаяСуммаБезСкидок=0, 0, Форма.СкидкаСумма / ОбщаяСуммаБезСкидок * 100);
	
КонецПроцедуры


Процедура ПосчитатьСуммуСтрокиСоСкидками(СтрокаТабличнойЧасти) Экспорт 
	
	СтрокаТабличнойЧасти.Сумма = СтрокаТабличнойЧасти.Количество * СтрокаТабличнойЧасти.Цена;
	СуммаПроцентовСкидки = 0;
	//+++ Дмгг, 01.05.2025, ONESOFT-4019  
	// Второй раз смотрю на этот баг. У СтрокиТЧ нет метода "Свойство()", а игнорирование проблемы её не исправляет
	//ЕстьОшибка = ложь;	 
	
//	Попытка
//	//Малышев 2025-01-07 ((-- 
////		Если СтрокаТабличнойЧасти.ПроцентАвтоматическойСкидки<>0 Тогда
//		Если СтрокаТабличнойЧасти.ПроцентАвтоматическойСкидки<>0 и СтрокаТабличнойЧасти.Свойство("ПроцентСкидкаЛояльности") Тогда
//			СтрокаТабличнойЧасти.ПроцентСкидкаЛояльности = 0;
//		КонецЕсли;	
//	исключение   
//		ЕстьОшибка = Истина;	
//	КонецПопытки;
	Если СтрокаТабличнойЧасти.ПроцентАвтоматическойСкидки<>0 и ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(СтрокаТабличнойЧасти, "ПроцентСкидкаЛояльности") Тогда
		СтрокаТабличнойЧасти.ПроцентСкидкаЛояльности = 0;
	КонецЕсли;
	//--- Дмгг, 01.05.2025, ONESOFT-4019	
	
	//+++ Дмгг, 01.05.2025, ONESOFT-4019  
	// Второй раз смотрю на этот баг. У СтрокиТЧ нет метода "Свойство()", а игнорирование проблемы её не исправляет
//		//Малышев 2025-03-08 ONESOFT-3749
//		//Сначала применим одну скидку
//		Попытка
//		СуммаПроцентовСкидки = 0
////			+ СтрокаТабличнойЧасти.ПроцентСкидкиПосСрокуГодности 
//			+ ?(СтрокаТабличнойЧасти.Свойство("ПроцентСкидкаЛояльности"), СтрокаТабличнойЧасти.ПроцентСкидкаЛояльности ,0)
////			+ СтрокаТабличнойЧасти.ПроцентСкидкиНаценки 
////			+ СтрокаТабличнойЧасти.ПроцентАвтоматическойСкидки
//			;
//		исключение   
//			ЕстьОшибка = Истина;	
//		КонецПопытки;
	СуммаПроцентовСкидки = 0
//		+ СтрокаТабличнойЧасти.ПроцентСкидкиПосСрокуГодности 
		+ ?(ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(СтрокаТабличнойЧасти, "ПроцентСкидкаЛояльности"), СтрокаТабличнойЧасти.ПроцентСкидкаЛояльности ,0)
//		+ СтрокаТабличнойЧасти.ПроцентСкидкиНаценки 
//		+ СтрокаТабличнойЧасти.ПроцентАвтоматическойСкидки
		;    
	//--- Дмгг, 01.05.2025, ONESOFT-4019
			
	СуммаПроцентовСкидки = Мин(100, СуммаПроцентовСкидки);
		
	СтрокаТабличнойЧасти.Сумма = СтрокаТабличнойЧасти.Сумма * (1 - СуммаПроцентовСкидки / 100);
		
	//потолм на нее накрутим другую скидку  
	//+++ Дмгг, 01.05.2025, ONESOFT-4019  
	// Второй раз смотрю на этот баг. У СтрокиТЧ нет метода "Свойство()", а игнорирование проблемы её не исправляет
	//	Попытка
	//		СуммаПроцентовСкидки = 0
	//			+ ?(СтрокаТабличнойЧасти.Свойство("ПроцентСкидкиПосСрокуГодности"),СтрокаТабличнойЧасти.ПроцентСкидкиПосСрокуГодности,0) 
	////			+ СтрокаТабличнойЧасти.ПроцентСкидкаЛояльности 
	//			+ ?(СтрокаТабличнойЧасти.Свойство("ПроцентАвтоматическойСкидки"),СтрокаТабличнойЧасти.ПроцентАвтоматическойСкидки,0)
	////			+ СтрокаТабличнойЧасти.ПроцентСкидкиНаценки 
	//			;
	// 	исключение   
	//		ЕстьОшибка = Истина;	
	//	КонецПопытки;
	СуммаПроцентовСкидки = 0
		+ ?(ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(СтрокаТабличнойЧасти, "ПроцентСкидкиПосСрокуГодности"),СтрокаТабличнойЧасти.ПроцентСкидкиПосСрокуГодности,0) 
	//	+ СтрокаТабличнойЧасти.ПроцентСкидкаЛояльности 
		+ ?(ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(СтрокаТабличнойЧасти, "ПроцентАвтоматическойСкидки"),СтрокаТабличнойЧасти.ПроцентАвтоматическойСкидки,0)
	//	+ СтрокаТабличнойЧасти.ПроцентСкидкиНаценки 
		;    
	//--- Дмгг, 01.05.2025, ONESOFT-4019

	СуммаПроцентовСкидки = Мин(100, СуммаПроцентовСкидки);


	//Малышев 2025-03-12 ONESOFT-3748
	Попытка
		Если ОбщегоНазначенияВызовСервера.ПолучитьЗначениеКонстанты("РассчитыватьРучнуюСкидкуСоСкидочнойЦены") Тогда
			СтрокаТабличнойЧасти.Сумма = СтрокаТабличнойЧасти.Сумма * (1 - СуммаПроцентовСкидки / 100);
			
			СуммаПроцентовСкидки = 0
				+ СтрокаТабличнойЧасти.ПроцентСкидкиНаценки 
			;		
			
		иначе
			СуммаПроцентовСкидки = СуммаПроцентовСкидки
				+ СтрокаТабличнойЧасти.ПроцентСкидкиНаценки 
			;		
		КонецЕсли;	
 	исключение   
		ЕстьОшибка = Истина;	
	КонецПопытки;	
	
	СуммаПроцентовСкидки = Мин(100, СуммаПроцентовСкидки);
	СтрокаТабличнойЧасти.Сумма = СтрокаТабличнойЧасти.Сумма * (1 - СуммаПроцентовСкидки / 100);
		
КонецПроцедуры      
