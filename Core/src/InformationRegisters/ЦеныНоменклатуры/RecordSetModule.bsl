
//Paganini_{
Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	ЗапретРедактированияСозданияИУдаленияЦенПрошлойДатой = УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(,"ЗапретРедактированияСозданияИУдаленияЦенПрошлойДатой");
	Если ЗапретРедактированияСозданияИУдаленияЦенПрошлойДатой Тогда
		Если ЭтотОбъект.Количество() = 0 И ЭтотОбъект.Модифицированность() = Истина Тогда
			МЗ = РегистрыСведений.ЦеныНоменклатуры.СоздатьМенеджерЗаписи();
			Для каждого ЭлементОтбора из ЭтотОбъект.Отбор Цикл
				МЗ[ЭлементОтбора.Имя] = ЭлементОтбора.Значение;
			КонецЦикла;
			МЗ.Прочитать();
			Если МЗ.Период < НачалоДня(ТекущаяДата()) Тогда
				ОбщегоНазначенияКлиентСервер.СообщитьПользователю(НСтр("ru = 'У текущего пользователя установлен ""Запрет редактирования, создания и удаления цен прошлой датой!'; ka = 'მოცემულ მომხმარებელს დაყენებული აქვს ""გასული თარიღით ფასების რედაქტირების, შექმნის და წაშლის აკრძალვა""!'; en = 'მოცემულ მომხმარებელს დაყენებული აქვს ""გასული თარიღით ფასების რედაქტირების, შექმნის და წაშლის აკრძალვა""!'"), , , , Отказ);
            КонецЕсли;
		Иначе
	        Для Каждого ТекЗапись Из ЭтотОбъект Цикл
	            Если ТекЗапись.Период < НачалоДня(ТекущаяДата()) Тогда
					ОбщегоНазначенияКлиентСервер.СообщитьПользователю(НСтр("ru = 'У текущего пользователя установлен ""Запрет редактирования и создания цен прошлой датой!'; ka = 'მოცემულ მომხმარებელს დაყენებული აქვს ""გასული თარიღით ფასების რედაქტირების და შექმნის აკრძალვა""!'; en = 'მოცემულ მომხმარებელს დაყენებული აქვს ""გასული თარიღით ფასების რედაქტირების და შექმნის აკრძალვა""!'"), , , , Отказ);
	            КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры
//}_Paganini