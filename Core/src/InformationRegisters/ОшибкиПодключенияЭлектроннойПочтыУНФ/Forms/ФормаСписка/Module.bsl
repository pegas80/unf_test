
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("УчетнаяЗапись") Тогда
		УстановитьОтборПоУчетнойЗаписи(Параметры.УчетнаяЗапись);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура УстановитьОтборПоУчетнойЗаписи(УчетнаяЗапись)
	
	Элементы.УчетнаяЗапись.Видимость = Ложь;
	Заголовок = СтрШаблон(
	НСтр("ka='Ошибки по учетной записи ""%1""';ru='Ошибки по учетной записи ""%1""';en='on account error ""%1""'"),
	УчетнаяЗапись);
	
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(
	Список,
	"УчетнаяЗапись",
	УчетнаяЗапись);
	
КонецПроцедуры

#КонецОбласти