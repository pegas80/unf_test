#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого Запись Из ЭтотОбъект Цикл
		Если Запись.Применяется Тогда
			Запись.Представление = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Запись.КодВычета,"Наименование");
		Иначе
				Запись.Представление =  НСтр("ka='სტანდარტული პირადი დაქვითვა არ გამოიყენება';ru='Стандартный личный вычет не применяется';en='Standard personal deduction does not apply'");
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

#КонецЕсли