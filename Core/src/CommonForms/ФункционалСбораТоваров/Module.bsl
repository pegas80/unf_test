
&НаКлиенте
Процедура ПарольПриИзменении(Элемент)
	ОчиститьСообщения();
	ПарольВерные = ПроверитьПароль();
	Элементы.ФункционалСбораТоваров.Доступность = ПарольВерные;
	Если НЕ ПарольВерные Тогда
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = НСТР("ka='პაროლი არასწორია!';ru='Пароль неверный!';en='Wrong password!'");
		Сообщение.Поле =  "Пароль";
		Сообщение.Сообщить();
	КонецЕсли;                                                                            
КонецПроцедуры

&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	Если НЕ ПарольВерные Тогда
		Отказ = Истина;
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = НСТР("ka='პაროლი არასწორია!';ru='Пароль неверный!';en='Wrong password!'");
		Сообщение.Поле =  "Пароль";
		Сообщение.Сообщить();
	КонецЕсли;
КонецПроцедуры

Функция ПроверитьПароль()
	
	Если Пароль = "dd163" Тогда
		Возврат Истина;
	Иначе 
		Возврат Ложь;
	КонецЕсли;	
	
КонецФункции	
