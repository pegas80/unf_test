
#Область ОбработчикиКомандФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	// Пропускаем инициализацию, чтобы гарантировать получение формы при передаче параметра "АвтоТест"
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	Параметры.Свойство("Идентификатор", Идентификатор);
	Параметры.Свойство("ДрайверОборудования", ДрайверОборудования);
	
	Заголовок = НСтр("ru='Оборудование:';ka='Оборудование:'") + Символы.НПП  + Строка(Идентификатор);
	
	ЦветТекста = ЦветаСтиля.ЦветТекстаФормы;
	ЦветОшибки = ЦветаСтиля.ЦветОтрицательногоЧисла;

	СпПорт = Элементы.Порт.СписокВыбора;
	Для Номер = 1 По 64 Цикл
		СпПорт.Добавить(Номер, "COM" + Формат(Номер, "ЧЦ=2; ЧДЦ=0; ЧН=0; ЧГ=0"));
	КонецЦикла;  
	
	СпИнтерфейс = Элементы.Интерфейс.СписокВыбора;
	СпИнтерфейс.Добавить(0, "RS-232");
	СпИнтерфейс.Добавить(1, "Ethernet");

    СпСкорость = Элементы.Скорость.СписокВыбора; 
	СпСкорость.Добавить(0,  "2400");
	СпСкорость.Добавить(1,  "4800");
	СпСкорость.Добавить(2,  "9600");
	СпСкорость.Добавить(3, "19200");
	СпСкорость.Добавить(4, "38400");
	СпСкорость.Добавить(5, "57600");
	СпСкорость.Добавить(6,"115200");
	
	времНаименование        = Неопределено;
	времИнтерфейс           = Неопределено;
	времПорт                = Неопределено;
	времСкорость            = Неопределено;
	времТаймаут             = Неопределено;
	времIPАдрес             = Неопределено;
	времАдресПрограмы       = Неопределено;
	//времПодразделение       = Неопределено;	
	времUDPПортПолучателя   = Неопределено;
	времUDPТаймаут          = Неопределено;
	времПароль              = Неопределено;
	времБыстраяЗагрузка     = Неопределено;
	
	Параметры.ПараметрыОборудования.Свойство("Интерфейс"        , времИнтерфейс);
	Параметры.ПараметрыОборудования.Свойство("Порт"             , времПорт);
	Параметры.ПараметрыОборудования.Свойство("Скорость"         , времСкорость);
	Параметры.ПараметрыОборудования.Свойство("Таймаут"          , времТаймаут);
	Параметры.ПараметрыОборудования.Свойство("IPАдрес"          , времIPАдрес);
	Параметры.ПараметрыОборудования.Свойство("АдресПрограмы"    , времАдресПрограмы);
	//Параметры.ПараметрыОборудования.Свойство("Подразделение"    , времПодразделение);	
	Параметры.ПараметрыОборудования.Свойство("UDPПортПолучателя", времUDPПортПолучателя);
	Параметры.ПараметрыОборудования.Свойство("UDPТаймаут"       , времUDPТаймаут);
	Параметры.ПараметрыОборудования.Свойство("Пароль"           , времПароль);
	Параметры.ПараметрыОборудования.Свойство("БыстраяЗагрузка"  , времБыстраяЗагрузка);
	Параметры.ПараметрыОборудования.Свойство("Наименование"     , времНаименование);
	                          
	Интерфейс          = ?(времИнтерфейс           = Неопределено, 0, времИнтерфейс);
	Порт               = ?(времПорт                = Неопределено, 1, времПорт);
	Скорость           = ?(времСкорость            = Неопределено, 2, времСкорость);
	Таймаут            = ?(времТаймаут             = Неопределено, 50, времТаймаут);
	IPАдрес            = ?(времIPАдрес             = Неопределено, "192.168.0.1", времIPАдрес);
	АдресПрограмы      = ?(времАдресПрограмы       = Неопределено, "", времАдресПрограмы);
	//Подразделение      = ?(времПодразделение       = Неопределено, "", времПодразделение);	
	UDPПортПолучателя  = ?(времUDPПортПолучателя   = Неопределено, 1111, времUDPПортПолучателя);
	UDPТаймаут         = ?(времUDPТаймаут          = Неопределено, 500, времUDPТаймаут);
	Пароль             = ?(времПароль              = Неопределено, "0030", времПароль);
	БыстраяЗагрузка    = ?(времБыстраяЗагрузка     = Неопределено, Ложь, времБыстраяЗагрузка);
	
	Наименование = ?(времНаименование = Неопределено, НСтр("ru='ШТРИХ-ПРИНТ';ka='ШТРИХ-ПРИНТ'"), времНаименование);
	
	//Элементы.ТестУстройства.Видимость    = (ПараметрыСеанса.РабочееМестоКлиента
	//                                        = Идентификатор.РабочееМесто);
	//Элементы.УстановитьДрайвер.Видимость = (ПараметрыСеанса.РабочееМестоКлиента
	//                                        = Идентификатор.РабочееМесто); 
	
	//УправлениеИнтерфеисом.ИзменитьКартинкыФормы(ЭтаФорма);
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)

	//ОбновитьИнформациюОДрайвере();

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ИнтерфейсПриИзменении(Элемент)
	
	ПроверкаДоступности();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ЗаписатьИЗакрытьВыполнить()
	
	НовыеЗначениеПараметров = Новый Структура;
	НовыеЗначениеПараметров.Вставить("Интерфейс"        , Интерфейс);
	НовыеЗначениеПараметров.Вставить("Порт"             , Порт);
	НовыеЗначениеПараметров.Вставить("Скорость"         , Скорость);
	НовыеЗначениеПараметров.Вставить("Таймаут"          , Таймаут);
	НовыеЗначениеПараметров.Вставить("IPАдрес"          , IPАдрес);
	НовыеЗначениеПараметров.Вставить("АдресПрограмы"          , АдресПрограмы);
	//НовыеЗначениеПараметров.Вставить("Подразделение"          , Подразделение);
	НовыеЗначениеПараметров.Вставить("Наименование"     , Наименование);
	
	Результат = Новый Структура;
	Результат.Вставить("Идентификатор", Идентификатор);
	Результат.Вставить("ПараметрыОборудования", НовыеЗначениеПараметров);
	
	Закрыть(Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьДрайверИзАрхиваПриЗавершении(Результат) Экспорт 
	
	ОбщегоНазначенияКлиентСервер.СообщитьПользователю(НСтр("ru='Установка драйвера завершена.';ka='Установка драйвера завершена.'")); 
	ОбновитьИнформациюОДрайвере();
	
КонецПроцедуры 

&НаКлиенте
Процедура УстановитьДрайверИзДистрибутиваПриЗавершении(Результат, Параметры) Экспорт 
	
	Если Результат Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(НСтр("ru='Установка драйвера завершена.';ka='Установка драйвера завершена.'")); 
		ОбновитьИнформациюОДрайвере();
	Иначе
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(НСтр("ru='При установке драйвера из дистрибутива произошла ошибка.';ka='При установке драйвера из дистрибутива произошла ошибка.'")); 
	КонецЕсли;

КонецПроцедуры 

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура ПроверкаДоступности()

	Элементы.Порт.Доступность          		= (Интерфейс = 0);
	Элементы.Скорость.Доступность      		= (Интерфейс = 0);
	Элементы.Таймаут.Доступность      		= (Интерфейс = 0);
	Элементы.IPАдрес.Доступность       		= Не (Интерфейс = 0);
	Элементы.АдресПрограмы.Доступность       		= Не (Интерфейс = 0);
              
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьИнформациюОДрайвере()

	ВходныеПараметры  = Неопределено;
	ВыходныеПараметры = Неопределено;

	времПараметрыУстройства = Новый Структура();
	времПараметрыУстройства.Вставить("Интерфейс"    		, Интерфейс);
	времПараметрыУстройства.Вставить("Порт"     	 		, Порт);
	времПараметрыУстройства.Вставить("Скорость" 	  		, Скорость);
	времПараметрыУстройства.Вставить("Таймаут"				, Таймаут);	
	времПараметрыУстройства.Вставить("IPАдрес"				, IPАдрес);
	времПараметрыУстройства.Вставить("АдресПрограмы"		, АдресПрограмы);
	//времПараметрыУстройства.Вставить("Подразделение"		, Подразделение);	
	времПараметрыУстройства.Вставить("Наименование"			, Наименование);
	
	
	Если МенеджерОборудованияКлиент.ВыполнитьДополнительнуюКоманду("ПолучитьВерсиюДрайвера",
	                                                               ВходныеПараметры,
	                                                               ВыходныеПараметры,
	                                                               Идентификатор,
	                                                               времПараметрыУстройства) Тогда
		Драйвер = ВыходныеПараметры[0];
		Версия  = ВыходныеПараметры[1];
	Иначе
		Драйвер = ВыходныеПараметры[2];
		Версия  = НСтр("ka='არ არის განსაზღვრული';ru='Не определена'");
	КонецЕсли;

	Элементы.Драйвер.ЦветТекста = ?(Драйвер = НСтр("ru='Не установлен';ka='Не установлен'"), ЦветОшибки, ЦветТекста);
	Элементы.Версия.ЦветТекста  = ?(Версия  = НСтр("ka='არ არის განსაზღვრული';ru='Не определена'"), ЦветОшибки, ЦветТекста);

	Элементы.УстановитьДрайвер.Доступность = Не (Драйвер = НСтр("ka='დაყენებულია';ru='Установлен'"));

	ПроверкаДоступности();
	
КонецПроцедуры

&НаКлиенте
Процедура АдресПрограмыНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.ВыборКаталога);        	
	
	Если Диалог.Выбрать() Тогда
		АдресПрограмы = Диалог.Каталог;
	КонецЕсли;
	
	СтандартнаяОбработка = Ложь;
	
КонецПроцедуры

#КонецОбласти