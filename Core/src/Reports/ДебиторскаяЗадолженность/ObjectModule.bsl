Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)

	//НастройкиОтчета = КомпоновщикНастроек.ПолучитьНастройки();
	//
	//ПараметрНачалоПериода = НастройкиОтчета.ПараметрыДанных.НайтиЗначениеПараметра(Новый ПараметрКомпоновкиДанных("НачалоПериода"));
	//ПараметрКонецПериода = НастройкиОтчета.ПараметрыДанных.НайтиЗначениеПараметра(Новый ПараметрКомпоновкиДанных("КонецПериода"));
	//
	//Если ПараметрНачалоПериода <> Неопределено И ПараметрНачалоПериода.Использование
	//	И ПараметрКонецПериода <> Неопределено И ПараметрКонецПериода.Использование
	//	И ТипЗнч(ПараметрНачалоПериода.Значение) = Тип("СтандартнаяДатаНачала")
	//	И ТипЗнч(ПараметрКонецПериода.Значение) = Тип("СтандартнаяДатаНачала")
	//	И ПараметрНачалоПериода.Значение <> Дата(1,1,1)//.Дата
	//	И ПараметрКонецПериода.Значение <> Дата(1,1,1)//.Дата
	//	И ПараметрНачалоПериода.Значение > ПараметрКонецПериода.Значение Тогда//.Дата .Дата
	//	
	//	Сообщение 			= Новый СообщениеПользователю;
	//	Сообщение.Текст	 	= НСтр("ru='Дата начала периода не должна превышать дату окончания!';ka='პერიოდის დაწყების თარიღი არ უნდა აღემატებოდეს დასრულების თარიღს!'");
	//	Сообщение.Сообщить();
	//	
	//	СтандартнаяОбработка = Ложь;
	//	Возврат;
	//	
	//КонецЕсли;
	//
	//Если Не ПараметрКонецПериода.Использование Или ПараметрКонецПериода.Значение= Дата(1,1,1)  Тогда //.Дата 
	//	Сообщение 			= Новый СообщениеПользователю;
	//	Сообщение.Текст	 	= НСтр("ru='Обязательно Заполнить дату окончания!';ka='აუცილებელია პერიოდის დასრულების თარიღის შევსება!'");
	//	Сообщение.Сообщить();
	//	СтандартнаяОбработка = Ложь;
	//	Возврат;
	//КонецЕсли;
	

КонецПроцедуры

