

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	ЗаполнениеОбъектовУНФ.ЗаполнитьДокумент(ЭтотОбъект, ДанныеЗаполнения);
КонецПроцедуры

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	Если Отказ Тогда
		Возврат;	
	КонецЕсли;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	МинимальныеОстаткиСкладаЗапасы.Ссылка КАК Регистратор,
		|	МинимальныеОстаткиСкладаЗапасы.Ссылка.Дата КАК Период,
		|	МинимальныеОстаткиСкладаЗапасы.Ссылка.СтруктурнаяЕдиница,
		|	МинимальныеОстаткиСкладаЗапасы.Номенклатура,
		|	МинимальныеОстаткиСкладаЗапасы.Количество
		|ИЗ
		|	Документ.МинимальныеОстаткиСклада.Запасы КАК МинимальныеОстаткиСкладаЗапасы
		|ГДЕ
		|	МинимальныеОстаткиСкладаЗапасы.Ссылка = &Ссылка";
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	ЗагружаемаяТаблица = Запрос.Выполнить().Выгрузить();
	
	
	Регистр_МинимальныеОстаткиСклада = РегистрыСведений.МинимальныеОстаткиСклада;
	НаборМинимальныеОстаткиСклада = Регистр_МинимальныеОстаткиСклада.СоздатьНаборЗаписей();
	НаборМинимальныеОстаткиСклада.Отбор.Регистратор.Установить(Ссылка);
	НаборМинимальныеОстаткиСклада.Загрузить(ЗагружаемаяТаблица);
	НаборМинимальныеОстаткиСклада.Записать(Истина);
	
КонецПроцедуры
