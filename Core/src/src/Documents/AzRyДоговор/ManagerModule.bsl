
Процедура Печать(ТабДок, Ссылка) Экспорт
	//{{_КОНСТРУКТОР_ПЕЧАТИ(ბეჭდვა)
	Макет = Документы.AzRyДоговор.ПолучитьМакет("Макет");
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	AzRyДоговор.Проект КАК проект,
	|	AzRyДоговор.ПроектыВерсия КАК проектверсия,
	|	AzRyДоговор.Организация КАК организация,
	|	AzRyДоговор.Контрагент КАК контрагент,
	|	AzRyДоговор.ДокументДоговора КАК объектдоговора,
	|	AzRyДоговор.Дата КАК дата,
	|	AzRyДоговор.МенеджерПроекта КАК менеджер,
	|	AzRyДоговор.Юрист КАК юрист,
	|	AzRyДоговор.Аудит КАК аудит,
	|	AzRyДоговор.Директор КАК директор,
	|	AzRyДоговор.Условия.(
	|		Дата КАК дата,
	|		Деиствие КАК событие,
	|		СтатьяДоговора КАК статья,
	|		Комментарий КАК коментарий,
	|		Ответственный КАК ответственный
	|	),
	|	AzRyДоговор.Сумма КАК сумма,
	|	AzRyДоговор.ТекстСуммы КАК текстСуммы,
	|	AzRyДоговор.ДатаДоговора КАК датадоговора,
	|	AzRyДоговор.Куриер КАК куриер,
	|	AzRyДоговор.ВалютаДокумента КАК валюта,
	|	AzRyДоговор.ГарантийныеУсловия,
	|	AzRyДоговор.ГарантийныеУсловияДатаОкончания,
	|	AzRyДоговор.ГарантийныеУсловияТекст,
	|	AzRyДоговор.ПереходКПоддержке,
	|	AzRyДоговор.ПереходКПоддержкеДатаОкончания,
	|	AzRyДоговор.ПереходКПоддержкеТекст,
	|	AzRyДоговор.ПереходВУслуги,
	|	AzRyДоговор.ПереходВУслугиДатаОкончания,
	|	AzRyДоговор.ПереходВУслугиТекст,
	|	AzRyДоговор.ПриобретениеИзСвоейКомпании,
	|	AzRyДоговор.ПриобретениеИзСвоейКомпанииНаимОрг,
	|	AzRyДоговор.ПриобретениеИзСвоейКомпанииДоговор,
	|	AzRyДоговор.ТипПродажи.(
	|		Ссылка,
	|		НомерСтроки,
	|		ТипПродажи.И,
	|		ПредметСоглашения,
	|		ТипПродажи,
	|		Количество,
	|		Цена,
	|		Валюта,
	|		ОтветственныйЗаПроизводство,
	|		Спецификация,
	|		ПериодичностьСписаниеЗапасов,
	|		Коментарий,
	|		Проект,
	|		Продукт,
	|		Экаунт,
	|		Склад КАК Склад
	|	),
	|	AzRyДоговор.РуководительНаправления КАК РуководительНаправления,
	|	AzRyДоговор.ДопКомментарий
	|ИЗ
	|	Документ.AzRyДоговор КАК AzRyДоговор
	|ГДЕ
	|	AzRyДоговор.Ссылка В(&Ссылка)";
	Запрос.Параметры.Вставить("Ссылка", Ссылка);
	Выборка = Запрос.Выполнить().Выбрать();

	ОбластьШапка = Макет.ПолучитьОбласть("Шапка");
	ОбластьТипПродажи = Макет.ПолучитьОбласть("ТипПродажи");
	ОбластьПодвал = Макет.ПолучитьОбласть("Подвал");
	ОбластьУсловия = Макет.ПолучитьОбласть("Условия");
	ОбластьДопКомментарий = Макет.ПолучитьОбласть("ДопКомментарий");
	ТабДок.Очистить();

	ВставлятьРазделительСтраниц = Ложь;
	Пока Выборка.Следующий() Цикл
		Если ВставлятьРазделительСтраниц Тогда
			ТабДок.ВывестиГоризонтальныйРазделительСтраниц();
		КонецЕсли;
	
		ОбластьШапка.Параметры.Заполнить(Выборка);
		ОбластьШапка.Параметры.валюта2 = Константы.ВалютаУчета.Получить();		
		СтрПарам = ПолучитьКурсВалюты(Выборка.дата, Выборка.валюта);
		ОбластьШапка.Параметры.сумма2 = Выборка.сумма * (СтрПарам.Курс / СтрПарам.Кратность); 
		ТабДок.Вывести(ОбластьШапка);
		
		ВыборкаТипПродажи = Выборка.ТипПродажи.Выбрать();
		Пока ВыборкаТипПродажи.Следующий() Цикл
			ОбластьТипПродажи.Параметры.Заполнить(ВыборкаТипПродажи);
			ТабДок.Вывести(ОбластьТипПродажи, Выборка.Уровень());
		КонецЦикла;
	
		ОбластьПодвал.Параметры.Заполнить(Выборка);
		ТабДок.Вывести(ОбластьПодвал, Выборка.Уровень());
		
		//ТабДок.Вывести(ОбластьУсловияШапка);
		ВыборкаУсловия = Выборка.Условия.Выбрать();
		Пока ВыборкаУсловия.Следующий() Цикл
			ОбластьУсловия.Параметры.Заполнить(ВыборкаУсловия);
			ТабДок.Вывести(ОбластьУсловия, ВыборкаУсловия.Уровень());
		КонецЦикла;
		
		ОбластьДопКомментарий.Параметры.ДопКоментарий = Выборка.ДопКомментарий;
		ТабДок.Вывести(ОбластьДопКомментарий);

		ВставлятьРазделительСтраниц = Истина;
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Функция ПолучитьКурсВалюты(Дата, ВалютаДокумента)

	СтрПарам = Новый Структура;
	
	ВалютаРасчетовКурсКратность = РегистрыСведений.КурсыВалют.ПолучитьПоследнее(Дата, Новый Структура("Валюта", ВалютаДокумента));
	
	СтрПарам.Вставить("Курс", ?(ВалютаРасчетовКурсКратность.Курс = 0, 0, ВалютаРасчетовКурсКратность.Курс));
	СтрПарам.Вставить("Кратность", ?(ВалютаРасчетовКурсКратность.Кратность = 0, 0, ВалютаРасчетовКурсКратность.Кратность));
	
	Возврат СтрПарам;
	
КонецФункции


