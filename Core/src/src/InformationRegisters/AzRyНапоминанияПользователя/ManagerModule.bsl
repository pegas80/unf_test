Функция ЗаписьИмеется(СтатусНапоминания, Получатель, Отправитель, Источник)

		

КонецФункции

Функция ОбновитьЗаписьПослеПроведенияДокумента(СтатусНапоминания, Источник) Экспорт

	НаборЗаписей = РегистрыСведений.AzRyНапоминанияПользователя.СоздатьНаборЗаписей();    
	НаборЗаписей.Отбор.СтатусНапоминания.Установить(СтатусНапоминания);
	НаборЗаписей.Отбор.Источник.Установить(Источник);
	НаборЗаписей.Прочитать();
	
	МенеджерЗаписи = РегистрыСведений.AzRyНапоминанияПользователя.СоздатьМенеджерЗаписи();
	
	Для Каждого ТекЗапись Из НаборЗаписей Цикл
		ЗаполнитьЗначенияСвойств(МенеджерЗаписи, ТекЗапись);
		МенеджерЗаписи.Прочитать();
		МенеджерЗаписи.СтатусНапоминания = Перечисления.AzRyСтатусНапоминания.Finnished;
		МенеджерЗаписи.Период = ТекущаяДата();
		МенеджерЗаписи.Записать();
	КонецЦикла;	

КонецФункции

 