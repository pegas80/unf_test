#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Процедура создает пустую временную таблицу изменения движений.
//
Процедура СоздатьПустуюВременнуюТаблицуИзменение(ДополнительныеСвойства) Экспорт
	
	Если НЕ ДополнительныеСвойства.Свойство("ДляПроведения")
	 ИЛИ НЕ ДополнительныеСвойства.ДляПроведения.Свойство("СтруктураВременныеТаблицы") Тогда
		Возврат;
	КонецЕсли;
	
	СтруктураВременныеТаблицы = ДополнительныеСвойства.ДляПроведения.СтруктураВременныеТаблицы;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ ПЕРВЫЕ 0
	|	ЦеныВозвратаНоменклатурDgFIFO.НомерСтроки КАК НомерСтроки,
	|	ЦеныВозвратаНоменклатурDgFIFO.Организация КАК Организация,
	|	ЦеныВозвратаНоменклатурDgFIFO.Подразделение КАК Подразделение,
	|	ЦеныВозвратаНоменклатурDgFIFO.Номенклатура КАК Номенклатура,
	|	ЦеныВозвратаНоменклатурDgFIFO.Характеристика КАК Характеристика,
	|	ЦеныВозвратаНоменклатурDgFIFO.Партия КАК Партия,
	|	ЦеныВозвратаНоменклатурDgFIFO.Документ КАК Документ,
	|	ЦеныВозвратаНоменклатурDgFIFO.Количество КАК КоличествоПередЗаписью,
	|	ЦеныВозвратаНоменклатурDgFIFO.Количество КАК КоличествоИзменение,
	|	ЦеныВозвратаНоменклатурDgFIFO.Количество КАК КоличествоПриЗаписи,
	|	ЦеныВозвратаНоменклатурDgFIFO.Сумма КАК СуммаПередЗаписью,
	|	ЦеныВозвратаНоменклатурDgFIFO.Сумма КАК СуммаИзменение,
	|	ЦеныВозвратаНоменклатурDgFIFO.Сумма КАК СуммаПриЗаписи
	|ПОМЕСТИТЬ ДвиженияЦеныВозвратаНоменклатурDgFIFOИзменение
	|ИЗ
	|	РегистрНакопления.ЦеныВозвратаНоменклатурDgFIFO КАК ЦеныВозвратаНоменклатурDgFIFO");
	
	Запрос.МенеджерВременныхТаблиц = СтруктураВременныеТаблицы.МенеджерВременныхТаблиц;
	РезультатЗапроса = Запрос.Выполнить();
	
	СтруктураВременныеТаблицы.Вставить("ДвиженияЦеныВозвратаНоменклатурDgFIFOИзменение", Ложь);
	
КонецПроцедуры // СоздатьПустуюВременнуюТаблицуИзменение()

#КонецОбласти

#КонецЕсли