
&НаКлиенте
Процедура СредныйКурс(Команда)
	СредныйКурсНаСервере();
КонецПроцедуры

&НаСервере
Процедура СредныйКурсНаСервере()
	
	Если ЗначениеЗаполнено(Объект.Период) И ЗначениеЗаполнено(Объект.Валюта) Тогда
		Ответ = РеквизитФормыВЗначение("Объект").ЗапросСреднийКурсЗаМесяц(Объект.Период, Объект.Валюта);	
	КонецЕсли; 
	
КонецПроцедуры
