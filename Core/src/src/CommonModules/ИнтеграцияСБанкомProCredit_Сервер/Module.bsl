
Функция ПолучитьТаблицуДвиженийПоСчетуВременноеХранилище() ЭКСПОРТ
    ПЕРЕМ РЕЗ;
	
	РЕЗ = Новый ТаблицаЗначений;  
	РЕЗ.Колонки.Добавить("PMTTYPE");
	РЕЗ.Колонки.Добавить("ID");
	РЕЗ.Колонки.Добавить("DOC_DATE");
	РЕЗ.Колонки.Добавить("CCY");
	РЕЗ.Колонки.Добавить("AMOUNT");
	РЕЗ.Колонки.Добавить("DESCRIP");
	РЕЗ.Колонки.Добавить("OP_NUM");
	РЕЗ.Колонки.Добавить("AMOUNT_EQU");
	РЕЗ.Колонки.Добавить("DOC_NUM");
	РЕЗ.Колонки.Добавить("DOC_DATE_IN_DOC");
	РЕЗ.Колонки.Добавить("OP_CODE");
	РЕЗ.Колонки.Добавить("OP_TYPE");// Kuzmich, 20.06.2024, (ONESOFT-3249) Помошник загрузки банковской выписки - ProCredit - тип операции
	РЕЗ.Колонки.Добавить("DEBIT");
	РЕЗ.Колонки.Добавить("DEBIT_NAME");
	РЕЗ.Колонки.Добавить("CREDIT");
	РЕЗ.Колонки.Добавить("CREDIT_NAME");
	РЕЗ.Колонки.Добавить("SENDER_ACC");
	РЕЗ.Колонки.Добавить("SENDER_ACC_NAME");
	РЕЗ.Колонки.Добавить("SENDER_BANK_CODE");
	РЕЗ.Колонки.Добавить("SENDER_BANK_NAME");
	РЕЗ.Колонки.Добавить("RECEIVER_ACC");
	РЕЗ.Колонки.Добавить("RECEIVER_ACC_NAME");
	РЕЗ.Колонки.Добавить("RECEIVER_BANK_CODE");
	РЕЗ.Колонки.Добавить("RECEIVER_BANK_NAME");
	РЕЗ.Колонки.Добавить("INTERMED_BANK_CODE");
	РЕЗ.Колонки.Добавить("INTERMED_BANK_NAME");
	РЕЗ.Колонки.Добавить("SENDER_TAX_CODE");
	РЕЗ.Колонки.Добавить("RECEIVER_TAX_CODE");
	РЕЗ.Колонки.Добавить("FULL_NAME");
	РЕЗ.Колонки.Добавить("REC_STATE");
	РЕЗ.Колонки.Добавить("PARENT_REC_ID");
	
	Возврат ПоместитьВоВременноеХранилище(РЕЗ, Новый УникальныйИдентификатор());
КонецФункции

Функция ПолучитьТаблицуДляКурсаВременноеХранилище() ЭКСПОРТ      
	ПЕРЕМ РЕЗ;
	
	РЕЗ = Новый ТаблицаЗначений;
	РЕЗ.Колонки.Добавить("Курс");  
	
	Возврат ПоместитьВоВременноеХранилище(РЕЗ, Новый УникальныйИдентификатор());
КонецФункции


// Функция - Создает и возвращает структуру параметров соединения для интеграции с банком ProCredit
// 
// Возвращаемое значение - Структура
// 	Поля структуры:
//      ProCredit_Login					 - Строка - логин пользователя
//      ProCredit_Password				 - Строка - пароль пользователя
//      ProCredit_ТестовыйРежим			 - Булево - работаем в тестовом режиме
//      ProCredit_Сертификат			 - СправочникСсылка.Файлы - ссылка на справочник с хранилищем двоичных данных сертификата
//      ProCredit_ПарольСертификата		 - Строка - пароль сертификата
//      ProCredit_РаботаЧерезСервер		 - Булево - предполагается, что соединение с сайтом TBC будет происходить с сервера 1С
//      ProCredit_ИспользоватьСертификат - Булево - ИСТИНА: соединение с банком с использованием сертификата подлинности; ЛОЖЬ - соединение с банком без сертификата подлинности
//      Nonce						 - Строка - Nonce
//      ДвоичныеДанныеСертификата	 - ДвоичныеДанные,АдресВоВременномХранилище - двоичные данные сертификата
//
Функция СтруктураПараметровСоединенияProCredit() ЭКСПОРТ
	ПЕРЕМ РЕЗ;

	РЕЗ = Новый Структура("ProCredit_Login,ProCredit_Password,ProCredit_ТестовыйРежим,OTP,ProCredit_РаботаЧерезСервер");
					
    Возврат РЕЗ;  
	
КонецФункции // ()

// Функция - Получить параметры соединения с банком ProCredit для пользователя
//
// Параметры:
//  ТекущийПользователь	 - СправочникСсылка.Пользователи - пользователь, для которого нужно получить настройки
// 
// Возвращаемое значение:
// 	Поля структуры:
//      ProCredit_Login					 - Строка - логин пользователя
//      ProCredit_Password				 - Строка - пароль пользователя
//      ProCredit_ТестовыйРежим			 - Булево - работаем в тестовом режиме
//      ProCredit_Сертификат			 - СправочникСсылка.Файлы - ссылка на справочник с хранилищем двоичных данных сертификата
//      ProCredit_ПарольСертификата		 - Строка - пароль сертификата
//      ProCredit_РаботаЧерезСервер		 - Булево - предполагается, что соединение с сайтом TBC будет происходить с сервера 1С
//      ProCredit_ИспользоватьСертификат - Булево - ИСТИНА: соединение с банком с использованием сертификата подлинности; ЛОЖЬ - соединение с банком без сертификата подлинности
//      Nonce						 - Строка - Nonce
//      ДвоичныеДанныеСертификата	 - ДвоичныеДанные,АдресВоВременномХранилище - двоичные данные сертификата
//
Функция ПолучитьПараметрыСоединенияProCredit(ТекущийПользователь = НЕОПРЕДЕЛЕНО) ЭКСПОРТ
	ПЕРЕМ РЕЗ;
	
	РЕЗ = СтруктураПараметровСоединенияProCredit();
	
	Если ТекущийПользователь = НЕОПРЕДЕЛЕНО Тогда
	
		ТекущийПользователь = Пользователи.ТекущийПользователь();
		
	ИначеЕсли ТипЗНЧ(ТекущийПользователь) = Тип("ПользовательИнформационнойБазы") Тогда
		
		ТекущийПользователь = Справочники.Пользователи.НайтиПоРеквизиту("ИдентификаторПользователяИБ", ТекущийПользователь.УникальныйИдентификатор);
		
	ИначеЕсли НЕ(ТипЗНЧ(ТекущийПользователь) = Тип("СправочникСсылка.Пользователи")) Тогда
		
		ТекущийПользователь = Пользователи.ТекущийПользователь();
	
	КонецЕсли;

	ProCredit_Login = ПланыВидовХарактеристик.НастройкиПользователей.ProCredit_Login;
	ProCredit_Password = ПланыВидовХарактеристик.НастройкиПользователей.ProCredit_Password; 
	ProCredit_ТестовыйРежим = ПланыВидовХарактеристик.НастройкиПользователей.ProCredit_ТестовыйРежим;
#Если ВебКлиент Тогда
	ProCredit_РаботаЧерезСервер = ИСТИНА;
#Иначе	
	ProCredit_РаботаЧерезСервер = ПланыВидовХарактеристик.НастройкиПользователей.ProCredit_РаботаЧерезСервер;
#КонецЕсли
	
	СписокНастроек = Новый СписокЗначений();
	СписокНастроек.Добавить(ProCredit_Login);	
	СписокНастроек.Добавить(ProCredit_Password);
	СписокНастроек.Добавить(ProCredit_ТестовыйРежим);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	НастройкиПользователей.Настройка КАК Настройка,
		|	НастройкиПользователей.Значение КАК Значение
		|ИЗ
		|	РегистрСведений.НастройкиПользователей КАК НастройкиПользователей
		|ГДЕ
		|	НастройкиПользователей.Настройка В(&Настройка)
		|	И НастройкиПользователей.Пользователь = &ТекущийПользователь";
	
	Запрос.УстановитьПараметр("Настройка", СписокНастроек);
	Запрос.УстановитьПараметр("ТекущийПользователь",ТекущийПользователь);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		Если ВыборкаДетальныеЗаписи.Настройка = ProCredit_Login Тогда
			РЕЗ.ProCredit_Login = ВыборкаДетальныеЗаписи.Значение;
		ИначеЕсли ВыборкаДетальныеЗаписи.Настройка = ProCredit_Password Тогда
			РЕЗ.ProCredit_Password = ВыборкаДетальныеЗаписи.Значение;
		ИначеЕсли ВыборкаДетальныеЗаписи.Настройка = ProCredit_ТестовыйРежим Тогда
			РЕЗ.ProCredit_ТестовыйРежим = ВыборкаДетальныеЗаписи.Значение;
		ИначеЕсли ВыборкаДетальныеЗаписи.Настройка = ProCredit_РаботаЧерезСервер Тогда
			РЕЗ.ProCredit_РаботаЧерезСервер = ВыборкаДетальныеЗаписи.Значение;
		КонецЕсли;
		
	КонецЦикла;
	
    Возврат РЕЗ;      
	
КонецФункции // ()     

Процедура ОбработатьДанныеДвиженийПоСчетуНаСервере(СтруктураОтвета,ТаблицаДвиженийПоСчетуАдресВременногоХранилища) ЭКСПОРТ
	
	Если СтруктураОтвета.Свойство("Message") И СтруктураОтвета.Message <> Неопределено Тогда 
		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(СтруктураОтвета.Message);
		
	Иначе
		
		ТаблицаДвиженийПоСчету = ПолучитьИзВременногоХранилища(ТаблицаДвиженийПоСчетуАдресВременногоХранилища);
		
		Если Структураответа.Свойство("Data") Тогда 
			
			Если ТипЗнч(Структураответа.Data) = Тип("Структура") Тогда   
				СтруктураData = Структураответа.Data;
				Если СтруктураData.Свойство("PMT") Тогда 
					Если ТипЗнч(СтруктураData.PMT) = Тип("Структура") Тогда
						СтруктураPMT = СтруктураData.PMT; 
						Если СтруктураPMT.Свойство("PMTINFO") Тогда 
							Если ТипЗнч(СтруктураPMT.PMTINFO) = Тип("Массив") Тогда
								МассивPMTINFO = СтруктураPMT.PMTINFO;
								Для Каждого ЭлемМассива Из МассивPMTINFO Цикл 
									
									СтруктураЗаполнения = ЭлемМассива;
									
									ДобавитьСтрокуТаблицыДвиженийПоСчету(СтруктураЗаполнения, ТаблицаДвиженийПоСчету);
									
								КонецЦикла;     
							КонецЕсли;
						КонецЕсли;
					КонецЕсли;
					
				ИначеЕсли СтруктураData.Свойство("Rate") Тогда
					
					СтруктураЗаполнения = Новый Структура("Курс", СтруктураData.Rate);
					ДобавитьСтрокуТаблицыДвиженийПоСчету(СтруктураЗаполнения, ТаблицаДвиженийПоСчету);
				
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры       

Процедура ДобавитьСтрокуТаблицыДвиженийПоСчету(СтруктураЗаполнения, ТаблицаДвиженийПоСчету)
	
	СтрокаТаблицаДвиженийПоСчету = ТаблицаДвиженийПоСчету.Добавить();
	
	ЗаполнитьЗначенияСвойств(СтрокаТаблицаДвиженийПоСчету, СтруктураЗаполнения);
	
КонецПроцедуры

Функция ПолучитьХэш(ТекстXMLДокумента) Экспорт 
	
	Хеш=Новый ХешированиеДанных(ХешФункция.CRC32);
	Хеш.Добавить(ТекстXMLДокумента);	
	
	Возврат Хеш.ХешСумма;
	
КонецФункции    

Функция ПолучитьСтруктуруJSONДокументов(СсылкиДокументов) Экспорт
	
	МассивДокументовДляJSON = Новый Массив;          
	
	КоллекцияЗначений = ПолучитьКоллекуциюЗначенийДокументаДляОтправки(СсылкиДокументов);     
	
	Если КоллекцияЗначений = Неопределено Тогда 
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(НСтр("ru = 'Данный вид операции не поддерживаеться'; ka = 'Данный вид операции не поддерживаеться'; en = 'Данный вид операции не поддерживаеться'"));
	КонецЕсли;
	
	Если ТипЗнч(КоллекцияЗначений) = Тип("Структура") Тогда
		ЭтоМеждународнаяТранзакция = КоллекцияЗначений.ЭтоМеждународнаяТранзакция;     
		ЭтоКазначейство = КоллекцияЗначений.ЭтоКазначейство;  
		ЭтоВалютнаяОперация = КоллекцияЗначений.ЭтоВалютнаяОперация;
	Иначе
		ЭтоМеждународнаяТранзакция = Ложь;     
		ЭтоКазначейство = Ложь; 
		ЭтоВалютнаяОперация = Ложь;
	КонецЕсли;
	
	Если ТипЗнч(КоллекцияЗначений) = Тип("Структура") Тогда
		
		СтруктураBANKACCTFROM = Новый Структура;
		СтруктураBANKACCTFROM.Вставить("ACCTID", КоллекцияЗначений.Отправитель_ACCTID);
		СтруктураBANKACCTFROM.Вставить("CURSYM", КоллекцияЗначений.Отправитель_CURSYM);  
		
		СтруктураBANKACCTTO = Новый Структура;
		СтруктураBANKACCTTO.Вставить("ACCTID", КоллекцияЗначений.Получатель_ACCTID);
		СтруктураBANKACCTTO.Вставить("CURSYM", КоллекцияЗначений.Получатель_CURSYM);  
		СтруктураBANKACCTTO.Вставить("ACCTNAME", КоллекцияЗначений.Получатель_ACCTNAME);
		СтруктураBANKACCTTO.Вставить("BANKID", КоллекцияЗначений.Получатель_BANKID);
		СтруктураBANKACCTTO.Вставить("BANKNAME", КоллекцияЗначений.Получатель_BANKNAME);
		СтруктураBANKACCTTO.Вставить("TAX_CODE", "");
		СтруктураBANKACCTTO.Вставить("COUNTRY", КоллекцияЗначений.Получатель_COUNTRY);
		СтруктураBANKACCTTO.Вставить("ADDRESS", КоллекцияЗначений.Получатель_ADDRESS);   
		
		СтруктураPMTINFO = Новый Структура;   
		Если ЭтоКазначейство Тогда    
			СтруктураPMTINFO.Вставить("DOCNUM", "");
		Иначе
			СтруктураPMTINFO.Вставить("DOCNUM", КоллекцияЗначений.DOCNUM);  
		КонецЕсли;	
		СтруктураPMTINFO.Вставить("BANKACCTFROM", СтруктураBANKACCTFROM);  
		СтруктураPMTINFO.Вставить("TRNAMT", КоллекцияЗначений.TRNAMT); 
		СтруктураPMTINFO.Вставить("BANKACCTTO", СтруктураBANKACCTTO);  
		СтруктураPMTINFO.Вставить("EXTERNAL_IDENTIFIER", КоллекцияЗначений.EXTERNAL_IDENTIFIER); 
		
		    
		
		Если ЭтоМеждународнаяТранзакция Тогда 
			
			СтруктураPMTINFO.Вставить("Memo", 							КоллекцияЗначений.Memo);
			СтруктураPMTINFO.Вставить("EXTINFO", 						КоллекцияЗначений.EXTINFO);
			СтруктураPMTINFO.Вставить("TRANSFER_WITH_THE_OPTION_OUR", 	КоллекцияЗначений.TRANSFER_WITH_THE_OPTION_OUR);
			СтруктураPMTINFO.Вставить("INTERMEDIARY_BANK", 				КоллекцияЗначений.INTERMEDIARY_BANK);
			СтруктураPMTINFO.Вставить("INTERMEDIARY_BANKCODE", 			КоллекцияЗначений.INTERMEDIARY_BANKCODE);
			СтруктураPMTINFO.Вставить("INTERMEDIARY_BANKANAME", 		КоллекцияЗначений.INTERMEDIARY_BANKANAME);  
			
		ИначеЕсли ЭтоКазначейство Тогда 
			
			СтруктураPMTINFO.Вставить("TREASCODE", 							КоллекцияЗначений.TREASCODE);
			СтруктураPMTINFO.Вставить("Memo", 								КоллекцияЗначений.Memo);
			СтруктураPMTINFO.Вставить("EXTINFO", 							КоллекцияЗначений.EXTINFO);
			СтруктураPMTINFO.Вставить("PAYMENT_IN_FAVOUR_OF_ANOTHER_PERSON",КоллекцияЗначений.PAYMENT_IN_FAVOUR_OF_ANOTHER_PERSON);  
			СтруктураPMTINFO.Вставить("TAX_PAYER_TAX_CODE", 				КоллекцияЗначений.TAX_PAYER_TAX_CODE);
			СтруктураPMTINFO.Вставить("TAX_PAYER_NAME", 					КоллекцияЗначений.TAX_PAYER_NAME);
			СтруктураPMTINFO.Вставить("FAST_TRANSFER", 						КоллекцияЗначений.FAST_TRANSFER); 
			
		Иначе
			
			СтруктураPMTINFO.Вставить("EXTINFO", 			КоллекцияЗначений.EXTINFO);
			СтруктураPMTINFO.Вставить("PAYMENT_TO_LEPL", 	"");
			СтруктураPMTINFO.Вставить("LEPL_TAX_CODE", 		КоллекцияЗначений.LEPL_TAX_CODE);//ДокументСсылка.Организация.ИНН);
			СтруктураPMTINFO.Вставить("LEPL_NAME", 			КоллекцияЗначений.LEPL_NAME);
			СтруктураPMTINFO.Вставить("FAST_TRANSFER", 		ИСТИНА);
			СтруктураPMTINFO.Вставить("Memo", 				КоллекцияЗначений.Memo);	
			
		КонецЕсли;
		
		МассивДокументовДляJSON.Добавить(СтруктураPMTINFO);
		
	ИначеЕсли ТипЗнч(КоллекцияЗначений) = Тип("Массив") Тогда
		
		Для Каждого СтруктураДокумента Из КоллекцияЗначений Цикл 
			СтруктураBANKACCTFROM = Новый Структура;
			СтруктураBANKACCTFROM.Вставить("ACCTID", СтруктураДокумента.Отправитель_ACCTID);
			СтруктураBANKACCTFROM.Вставить("CURSYM", СтруктураДокумента.Отправитель_CURSYM);  
			
			СтруктураBANKACCTTO = Новый Структура;
			СтруктураBANKACCTTO.Вставить("ACCTID", СтруктураДокумента.Получатель_ACCTID);
			СтруктураBANKACCTTO.Вставить("CURSYM", СтруктураДокумента.Получатель_CURSYM);  
			СтруктураBANKACCTTO.Вставить("ACCTNAME", СтруктураДокумента.Получатель_ACCTNAME);
			СтруктураBANKACCTTO.Вставить("BANKID", СтруктураДокумента.Получатель_BANKID);
			СтруктураBANKACCTTO.Вставить("BANKNAME", СтруктураДокумента.Получатель_BANKNAME);
			СтруктураBANKACCTTO.Вставить("TAX_CODE", "");
			СтруктураBANKACCTTO.Вставить("COUNTRY", "");
			СтруктураBANKACCTTO.Вставить("ADDRESS", "");   
			
			СтруктураPMTINFO = Новый Структура; 
			СтруктураPMTINFO.Вставить("DOCNUM", СтруктураДокумента.DOCNUM); 
			СтруктураPMTINFO.Вставить("BANKACCTFROM", СтруктураBANKACCTFROM);  
			СтруктураPMTINFO.Вставить("TRNAMT", СтруктураДокумента.TRNAMT); 
			СтруктураPMTINFO.Вставить("BANKACCTTO", СтруктураBANKACCTTO);  
			СтруктураPMTINFO.Вставить("EXTERNAL_IDENTIFIER", СтруктураДокумента.EXTERNAL_IDENTIFIER); 
			
			СтруктураPMTINFO.Вставить("EXTINFO", СтруктураДокумента.EXTINFO);
			СтруктураPMTINFO.Вставить("PAYMENT_TO_LEPL", "");
			СтруктураPMTINFO.Вставить("LEPL_TAX_CODE", СтруктураДокумента.LEPL_TAX_CODE);//ДокументСсылка.Организация.ИНН);
			СтруктураPMTINFO.Вставить("LEPL_NAME", СтруктураДокумента.LEPL_NAME);
			СтруктураPMTINFO.Вставить("FAST_TRANSFER", ИСТИНА);
			СтруктураPMTINFO.Вставить("Memo", СтруктураДокумента.Memo);
			
			//Если хоть одна строка тогда весь пакет в международную транзакцию
			Если НЕ ЭтоВалютнаяОперация И СтруктураДокумента.ЭтоВалютнаяОперация Тогда  
				ЭтоВалютнаяОперация = СтруктураДокумента.ЭтоВалютнаяОперация 
			КонецЕсли;
			
			МассивДокументовДляJSON.Добавить(СтруктураPMTINFO);    
			
		КонецЦикла;
	КонецЕсли;
	
	СтруктураPMTRQ = Неопределено;
	
	Если ЭтоМеждународнаяТранзакция ИЛИ ЭтоВалютнаяОперация Тогда 
		СтруктураPMTRQ = Новый Структура("PMTINFO,PMTINFO1,PMTINFO2", Неопределено, Неопределено, МассивДокументовДляJSON); 
	ИначеЕсли ЭтоКазначейство Тогда 
		СтруктураPMTRQ = Новый Структура("PMTINFO,PMTINFO1,PMTINFO2", Неопределено, МассивДокументовДляJSON, Неопределено);
	Иначе
		СтруктураPMTRQ = Новый Структура("PMTINFO,PMTINFO1,PMTINFO2", МассивДокументовДляJSON, Неопределено, Неопределено);	
	КонецЕсли;
	
	СтруктураОтправки = Новый Структура("TRNUID,PMTRQ","",СтруктураPMTRQ);
	
	Возврат СтруктураОтправки;
	
КонецФункции

Функция ПолучитьКоллекуциюЗначенийДокументаДляОтправки(ДокументСсылка)
	
	//УчетнаяПолитика = СлужебныйМодульЗаполнениеОбъектов.УчетнаяПолитика(ДокументСсылка.Организация,	ДокументСсылка.Дата);
	Если ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.РасходСоСчета") 
								И (ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.НаРасходы
								//ИЛИ ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.ПрочиеРасчеты
								//ИЛИ ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.РасчетыПоКредитам
								//ИЛИ ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.ВыдачаКредитаКонтрагенту
								//ИЛИ ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.ВыдачаЗаймаСотруднику
								ИЛИ ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.Прочее
								//ИЛИ ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.Подотчетнику
								//ИЛИ ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.ПенсионномуФонду
								ИЛИ ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.СнятиеНаличных) Тогда 
								
		Возврат Неопределено;
								
	КонецЕсли;
								
	ЭтоКазначейство = ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.РасходСоСчета") И ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.Налоги;
	ЭтоЗарплата = ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.РасходСоСчета") И ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.Зарплата;
	ЭтоКонвертация = ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.Конвертация");
	ЭтоДокументыДляМеждународныхТранзакций = ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.РасходСоСчета"); 
	ЭтоПереводНаДругойСчет = ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.РасходСоСчета") И ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.ПереводНаДругойСчет;
	НацВалюта = УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеКонстанты("НациональнаяВалюта");											
	ЭтоМеждународнаяТранзакция = ЛОЖЬ; 
	
	УИД_Док = ДокументСсылка.УникальныйИдентификатор();
	
	КоллекцияВозврата = Неопределено;    
	
	Если НЕ ЭтоЗарплата Тогда  
		
		КоллекцияВозврата = Новый Структура;  
		
		КоллекцияВозврата.Вставить("LEPL_NAME", 				ДокументСсылка.Организация.Наименование);
		Если ЭтоКонвертация Тогда 
			КоллекцияВозврата.Вставить("Memo", 					ДокументСсылка.Комментарий);
		Иначе
			КоллекцияВозврата.Вставить("Memo", 					ДокументСсылка.НазначениеПлатежа);
		КонецЕсли;
		КоллекцияВозврата.Вставить("EXTINFO", 					ДокументСсылка.Комментарий);  
		КоллекцияВозврата.Вставить("DOCNUM", 					ДокументСсылка.Номер); 
		КоллекцияВозврата.Вставить("LEPL_TAX_CODE", 			ДокументСсылка.Организация.ИНН); 
		КоллекцияВозврата.Вставить("EXTERNAL_IDENTIFIER",		УИД_Док);   
		КоллекцияВозврата.Вставить("ЭтоМеждународнаяТранзакция",ЛОЖЬ);
		КоллекцияВозврата.Вставить("ЭтоКазначейство",			ЭтоКазначейство);
		КоллекцияВозврата.Вставить("ЭтоВалютнаяОперация",		ЛОЖЬ);
		
		Если НЕ ЭтоКонвертация И ДокументСсылка.ВалютаДенежныхСредств <> НацВалюта Тогда  
			КоллекцияВозврата.ЭтоВалютнаяОперация = Истина;   
		КонецЕсли;
		
	КонецЕсли;                                           
	
	Если ЭтоКонвертация Тогда 
		
		Если ДокументСсылка.ВидОперации <> перечисления.ВидыОперацийКонвертация.СоСчета Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		КоллекцияВозврата.Вставить("Отправитель_ACCTID", 		Лев(ДокументСсылка.Отправитель.НомерСчета,22));
		КоллекцияВозврата.Вставить("Отправитель_CURSYM", 		ДокументСсылка.Отправитель.ВалютаДенежныхСредств.Наименование);
		КоллекцияВозврата.Вставить("Получатель_ACCTID", 		Лев(ДокументСсылка.Получатель.НомерСчета,22));    
		
		
		КоллекцияВозврата.Вставить("Получатель_CURSYM", 	ДокументСсылка.Получатель.ВалютаДенежныхСредств.Наименование);
		КоллекцияВозврата.Вставить("Получатель_ACCTNAME", 	ДокументСсылка.Получатель.НомерСчета);
		КоллекцияВозврата.Вставить("Получатель_BANKID", 	ДокументСсылка.Получатель.Банк.СВИФТБИК);
		КоллекцияВозврата.Вставить("Получатель_BANKNAME", 	ДокументСсылка.Получатель.Банк.Наименование); 
		
		ТипКИ = Перечисления.ТипыКонтактнойИнформации.Адрес;
		ВидКИ = Справочники.ВидыКонтактнойИнформации.ЮрАдресОрганизации;
		ТаблицаКИ = ДокументСсылка.Организация.КонтактнаяИнформация;
		ДополнитьСтруктуруСтранойАдресом(ТаблицаКИ, КоллекцияВозврата, ТипКИ, ВидКИ);
		
		Если ДокументСсылка.ВалютаОтправитель <> НацВалюта ИЛИ ДокументСсылка.ВалютаПолучатель <> НацВалюта Тогда  
			КоллекцияВозврата.ЭтоВалютнаяОперация = Истина;   
		КонецЕсли;
		
		КоллекцияВозврата.Вставить("TRNAMT", ДокументСсылка.СуммаКонвертации);
		
	ИначеЕсли ЭтоПереводНаДругойСчет Тогда   
		
		КоллекцияВозврата.Вставить("Отправитель_ACCTID", 		Лев(ДокументСсылка.БанковскийСчет.НомерСчета,22));
		КоллекцияВозврата.Вставить("Отправитель_CURSYM", 		ДокументСсылка.БанковскийСчет.ВалютаДенежныхСредств.Наименование);
		ВлделецСчетаОтправитель = ДокументСсылка.БанковскийСчет.Владелец;
		СВИФТБИК_Получатель = "";
		
		СВИФТБИК_Получатель = ДокументСсылка.СчетКонтрагента.Банк.СВИФТБИК;    
		ВлделецСчетаПолучатель = ДокументСсылка.СчетКонтрагента.Владелец;
		КоллекцияВозврата.Вставить("Получатель_ACCTID", 		Лев(ДокументСсылка.СчетКонтрагента.НомерСчета,22));
		КоллекцияВозврата.Вставить("Получатель_CURSYM", 		ДокументСсылка.СчетКонтрагента.ВалютаДенежныхСредств.Наименование);
		КоллекцияВозврата.Вставить("Получатель_ACCTNAME", 		ДокументСсылка.СчетКонтрагента.НомерСчета);
		КоллекцияВозврата.Вставить("Получатель_BANKID", 		СВИФТБИК_Получатель);
		КоллекцияВозврата.Вставить("Получатель_BANKNAME", 		ДокументСсылка.СчетКонтрагента.Банк.Наименование);  
		
		ТипКИ = Перечисления.ТипыКонтактнойИнформации.Адрес;
		ВидКИ = Справочники.ВидыКонтактнойИнформации.ЮрАдресОрганизации;
		ТаблицаКИ = ДокументСсылка.Организация.КонтактнаяИнформация;
		ДополнитьСтруктуруСтранойАдресом(ТаблицаКИ, КоллекцияВозврата, ТипКИ, ВидКИ); 
		
		Если ДокументСсылка.ВалютаДенежныхСредств <> НацВалюта 
			И (СВИФТБИК_Получатель <> "MIBGGE22" И ВлделецСчетаОтправитель = ВлделецСчетаПолучатель)
			ИЛИ (СВИФТБИК_Получатель = "MIBGGE22" И ВлделецСчетаОтправитель <> ВлделецСчетаПолучатель) Тогда  
			КоллекцияВозврата.ЭтоВалютнаяОперация = Истина;
		Иначе
			КоллекцияВозврата.ЭтоВалютнаяОперация = ЛОЖЬ;	
		КонецЕсли;
		
		КоллекцияВозврата.Вставить("TRNAMT", 				ДокументСсылка.СуммаДокумента);
		
	ИначеЕсли ЭтоКазначейство Тогда
		
		КоллекцияВозврата.Вставить("Отправитель_ACCTID", 	Лев(ДокументСсылка.БанковскийСчет.НомерСчета,22));
		КоллекцияВозврата.Вставить("Отправитель_CURSYM", 	ДокументСсылка.БанковскийСчет.ВалютаДенежныхСредств.Наименование);
		КоллекцияВозврата.Вставить("Получатель_ACCTID", 	Лев(ДокументСсылка.КодКазначейства.БанковскийСчет.НомерСчета,22));
		КоллекцияВозврата.Вставить("Получатель_CURSYM", 	ДокументСсылка.КодКазначейства.БанковскийСчет.ВалютаДенежныхСредств.Наименование);
		КоллекцияВозврата.Вставить("Получатель_ACCTNAME", 	ДокументСсылка.КодКазначейства.Наименование);
		КоллекцияВозврата.Вставить("Получатель_BANKID", 	ДокументСсылка.КодКазначейства.БанковскийСчет.Банк.СВИФТБИК);
		КоллекцияВозврата.Вставить("Получатель_BANKNAME", 	ДокументСсылка.КодКазначейства.БанковскийСчет.Банк.Наименование); 
		КоллекцияВозврата.Вставить("TRNAMT", 				ДокументСсылка.СуммаДокумента);
		
		ТипКИ = Перечисления.ТипыКонтактнойИнформации.Адрес;
		ВидКИ = Справочники.ВидыКонтактнойИнформации.ЮрАдресКонтрагента;
		ТаблицаКИ = ДокументСсылка.Контрагент.КонтактнаяИнформация;
		ДополнитьСтруктуруСтранойАдресом(ТаблицаКИ, КоллекцияВозврата, ТипКИ, ВидКИ);
		
		Если ЭтоКазначейство Тогда  
			
			КоллекцияВозврата.Вставить("TREASCODE", 							Лев(ДокументСсылка.КодКазначейства.БанковскийСчет.НомерСчета,22));
			КоллекцияВозврата.EXTINFO = "";
			КоллекцияВозврата.Вставить("PAYMENT_IN_FAVOUR_OF_ANOTHER_PERSON",	ЛОЖЬ);
			КоллекцияВозврата.Вставить("TAX_PAYER_TAX_CODE",                    ДокументСсылка.Организация.ИНН);
			КоллекцияВозврата.Вставить("TAX_PAYER_NAME", 						ДокументСсылка.Организация.Наименование);
			КоллекцияВозврата.Вставить("FAST_TRANSFER", 						Истина);   
			
		КонецЕсли;     
		
		Если ДокументСсылка.ВалютаДенежныхСредств <> НацВалюта Тогда  
			КоллекцияВозврата.ЭтоВалютнаяОперация = Истина;   
		КонецЕсли;
		
	ИначеЕсли ЭтоЗарплата Тогда      
		
		КоллекцияВозврата = Новый Массив;
		
		Для Каждого СтрокВыплаты Из ДокументСсылка.ВыплатаЗаработнойПлаты Цикл 
			
			Для Каждого СтроСотрудник Из СтрокВыплаты.Ведомость.Сотрудники Цикл   
				
				СтруктураЗаполнения = Новый Структура; 
				
				СтруктураЗаполнения.Вставить("LEPL_NAME", 			ДокументСсылка.Организация.Наименование);
				СтруктураЗаполнения.Вставить("Memo", 				ДокументСсылка.НазначениеПлатежа);
				СтруктураЗаполнения.Вставить("EXTINFO", 			ДокументСсылка.Комментарий);  
				СтруктураЗаполнения.Вставить("DOCNUM", 				ДокументСсылка.Номер); 
				СтруктураЗаполнения.Вставить("LEPL_TAX_CODE", 		ДокументСсылка.Организация.ИНН); 
				СтруктураЗаполнения.Вставить("EXTERNAL_IDENTIFIER",	Строка(УИД_Док) + "," + СтрокВыплаты.НомерСтроки);
				
				СтруктураЗаполнения.Вставить("Отправитель_ACCTID", 	Лев(ДокументСсылка.БанковскийСчет.НомерСчета,22));
				СтруктураЗаполнения.Вставить("Отправитель_CURSYM", 	ДокументСсылка.БанковскийСчет.ВалютаДенежныхСредств.Наименование);
				СтруктураЗаполнения.Вставить("Получатель_ACCTID", 	Лев(СтроСотрудник.Сотрудник.БанковскийСчетПоУмолчанию.НомерСчета,22));
				СтруктураЗаполнения.Вставить("Получатель_CURSYM", 	СтроСотрудник.Сотрудник.БанковскийСчетПоУмолчанию.ВалютаДенежныхСредств.Наименование);
				СтруктураЗаполнения.Вставить("Получатель_ACCTNAME", СтроСотрудник.Сотрудник.Наименование);
				СтруктураЗаполнения.Вставить("Получатель_BANKID", 	СтроСотрудник.Сотрудник.БанковскийСчетПоУмолчанию.Банк.СВИФТБИК);
				СтруктураЗаполнения.Вставить("Получатель_BANKNAME", СтроСотрудник.Сотрудник.БанковскийСчетПоУмолчанию.Банк.Наименование); 
				СтруктураЗаполнения.Вставить("TRNAMT", 				СтроСотрудник.СуммаПлатежа);  
				//СтруктураЗаполнения.Вставить("Получатель_COUNTRY", 	"");  
				//СтруктураЗаполнения.Вставить("Получатель_ADDRESS", 	"");       
				
				ТипКИ = Перечисления.ТипыКонтактнойИнформации.Адрес;
				ВидКИ = Справочники.ВидыКонтактнойИнформации.АдресФизЛицаПоПрописке;
				ТаблицаКИ = СтроСотрудник.Сотрудник.Физлицо.КонтактнаяИнформация;
				ДополнитьСтруктуруСтранойАдресом(ТаблицаКИ, СтруктураЗаполнения, ТипКИ, ВидКИ);
				
				Если ДокументСсылка.ВалютаДенежныхСредств <> НацВалюта Тогда  
					СтруктураЗаполнения.Вставить("ЭтоВалютнаяОперация",	Истина);     
				Иначе         
					СтруктураЗаполнения.Вставить("ЭтоВалютнаяОперация",	ЛОЖЬ);
				КонецЕсли;
				
				КоллекцияВозврата.Добавить(СтруктураЗаполнения);       
				
			КонецЦикла;
		КонецЦикла;     
		
	ИначеЕсли ЭтоДокументыДляМеждународныхТранзакций Тогда
		
		КоллекцияВозврата.Вставить("Отправитель_ACCTID", 	Лев(ДокументСсылка.БанковскийСчет.НомерСчета,22));
		КоллекцияВозврата.Вставить("Отправитель_CURSYM", 	ДокументСсылка.БанковскийСчет.ВалютаДенежныхСредств.Наименование);
		КоллекцияВозврата.Вставить("Получатель_ACCTID", 	Лев(ДокументСсылка.СчетКонтрагента.НомерСчета,22));
		КоллекцияВозврата.Вставить("Получатель_CURSYM", 	ДокументСсылка.СчетКонтрагента.ВалютаДенежныхСредств.Наименование);
		Если ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.ВыдачаЗаймаСотруднику
			ИЛИ ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.Подотчетнику Тогда 
			КоллекцияВозврата.Вставить("Получатель_ACCTNAME", 	ДокументСсылка.Подотчетник.Наименование);
		Иначе
			КоллекцияВозврата.Вставить("Получатель_ACCTNAME", 	ДокументСсылка.Контрагент.НаименованиеПолное);
		КонецЕсли;
		КоллекцияВозврата.Вставить("Получатель_BANKID", 	ДокументСсылка.СчетКонтрагента.Банк.СВИФТБИК);
		КоллекцияВозврата.Вставить("Получатель_BANKNAME", 	ДокументСсылка.СчетКонтрагента.Банк.Наименование); 
		КоллекцияВозврата.Вставить("TRNAMT", 				ДокументСсылка.СуммаДокумента);
		
		ЭтоМеждународнаяТранзакция = ПроверитНаМеждународнуюОтправкуСчетПолучателя(ДокументСсылка.СчетКонтрагента); 
		
		Если ЭтоМеждународнаяТранзакция Тогда      
			
			КоллекцияВозврата.ЭтоМеждународнаяТранзакция = ИСТИНА;     
			
			КоллекцияВозврата.Вставить("TRANSFER_WITH_THE_OPTION_OUR", 	ЛОЖЬ);
			КоллекцияВозврата.Вставить("INTERMEDIARY_BANK", 		ДокументСсылка.ПромежуточныйБанк.Наименование); 
			КоллекцияВозврата.Вставить("INTERMEDIARY_BANKCODE", 	ДокументСсылка.ПромежуточныйБанк.СВИФТБИК);
			КоллекцияВозврата.Вставить("INTERMEDIARY_BANKANAME", 	ДокументСсылка.ПромежуточныйБанк.Наименование);  
			
		КонецЕсли;    
		
		
		ТипКИ = Перечисления.ТипыКонтактнойИнформации.Адрес;
		ВидКИ = Справочники.ВидыКонтактнойИнформации.ЮрАдресКонтрагента;
		ТаблицаКИ = ДокументСсылка.Контрагент.КонтактнаяИнформация;
		ДополнитьСтруктуруСтранойАдресом(ТаблицаКИ, КоллекцияВозврата, ТипКИ, ВидКИ);
		
		Если ДокументСсылка.ВалютаДенежныхСредств <> НацВалюта Тогда  
			КоллекцияВозврата.ЭтоВалютнаяОперация = Истина;   
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат КоллекцияВозврата;
	
КонецФункции // ПолучитьСтруктураДокументаДляОтправки(СсылкиДокументов)()

Процедура ДополнитьСтруктуруСтранойАдресом(ТаблицаКИ, СтруктураДополнения, ТипКИ, ВидКИ)

	ПоискАдреса = ТаблицаКИ.НайтиСтроки(Новый Структура("Тип,Вид", ТипКИ, ВидКИ));
	
	Если ПоискАдреса.Количество() > 0 Тогда  
		Страна = "";
		СтрокаАдреса = ПоискАдреса[0];   
		СтранаТекст = СтрокаАдреса.Страна; 
		Если ЗначениеЗаполнено(СтранаТекст) Тогда 
			ПоискСтраныСсылка = Справочники.СтраныМира.НайтиПоНаименованию(СтранаТекст);
			Если ЗначениеЗаполнено(ПоискСтраныСсылка) Тогда 
				Страна = ПоискСтраныСсылка.КодАльфа3;
			Иначе
				Страна = СтранаТекст;
			КонецЕсли;
		Иначе
			Страна = СтранаТекст;
		КонецЕсли;
		
		СтруктураДополнения.Вставить("Получатель_COUNTRY", 	Страна);  
		СтруктураДополнения.Вставить("Получатель_ADDRESS", 	СтрокаАдреса.Представление);
	Иначе
		СтруктураДополнения.Вставить("Получатель_COUNTRY", 	"");  
		СтруктураДополнения.Вставить("Получатель_ADDRESS", 	"");
	КонецЕсли;	
	
КонецПроцедуры


Функция ПолучитьМассив_УИД_Документа(ДокументСсылка) ЭКСПОРТ
	ПЕРЕМ РЕЗ;
	
	РЕЗ = Новый Массив();
	
	Банк = ПолучитьБанкИзСсылкиНаДокумент(ДокументСсылка);
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	УИДДокументовВИнтернетБанке.ДокументСсылка КАК ДокументСсылка,
		|	ЕСТЬNULL(УИДДокументовВИнтернетБанке.УИДДокументаВБанке, """""""") КАК УИД
		|ИЗ
		|	РегистрСведений.УИДДокументовВИнтернетБанкеНовый КАК УИДДокументовВИнтернетБанке
		|ГДЕ
		|	УИДДокументовВИнтернетБанке.ДокументСсылка = &ДокументСсылка
		|	И УИДДокументовВИнтернетБанке.Банк = &Банк";
	
	Запрос.УстановитьПараметр("Банк", Банк);
	Запрос.УстановитьПараметр("ДокументСсылка", ДокументСсылка);
	
	РезультатЗапроса = Запрос.Выполнить();
	Если НЕ РезультатЗапроса.Пустой() Тогда
		РЕЗ = РезультатЗапроса.Выгрузить().ВыгрузитьКолонку("УИД");
	КонецЕсли;
	
	Возврат РЕЗ;
	
КонецФункции       

Функция ПолучитьБанкИзСсылкиНаДокумент(ДокументСсылка, БанкПоУмолчанию=НЕОПРЕДЕЛЕНО)
	ПЕРЕМ РЕЗ;
	
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДокументСсылка, "БанковскийСчетКасса") Тогда
		РЕЗ = ДокументСсылка.БанковскийСчетКасса.Банк;
	ИначеЕсли ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДокументСсылка, "Отправитель") Тогда
		РЕЗ = ДокументСсылка.Отправитель.Банк;
	ИначеЕсли ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДокументСсылка, "БанковскийСчетКассаОтправитель") Тогда
		РЕЗ = ДокументСсылка.БанковскийСчетКассаОтправитель.Банк;
	ИначеЕсли ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДокументСсылка, "БанковскийСчет") Тогда
		РЕЗ = ДокументСсылка.БанковскийСчет.Банк;
	ИначеЕсли ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДокументСсылка, "Банк") Тогда
		РЕЗ = ДокументСсылка.Банк;
	Иначе
		РЕЗ = БанкПоУмолчанию;
	КонецЕсли;
	
	Возврат РЕЗ;
КонецФункции

Процедура ОбработатьВыгрузкуДокументовВБанкНаСервере(СтруктураОтвета,ТаблицаПозицийАдресВременногоХранилища,ID_Отправки, ДокументСсылка) ЭКСПОРТ
	
	КолвоВыгруженныхДоков = 0;
		
	Если СтруктураОтвета.Свойство("ТекстОшибки") Тогда 
		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(СтруктураОтвета.ТекстОшибки);
		
	Иначе
		
		Если ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.РасходСоСчета") 
				И (ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.Зарплата 
				ИЛИ ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.ЗарплатаВРазрезеСотрудников) Тогда
			
			ТаблицаПозиций = ПолучитьИзВременногоХранилища(ТаблицаПозицийАдресВременногоХранилища);
			
			МассивДокументов = Новый Массив;      
			Если СтруктураОтвета.Свойство("Data") Тогда    
				СтруктураData = СтруктураОтвета.Data;
				Если СтруктураData.Свойство("TransfersStatus") Тогда
					МассивДокументов = СтруктураData.TransfersStatus; 
				КонецЕсли;
			КонецЕсли;   
			
			Для Каждого ЭлементМассива Из МассивДокументов Цикл 
				
				Статус = ЭлементМассива.TransferStatus;
				УИДДокумента = ЭлементМассива.REC_ID; 
				ПозицияРазделителя = СтрНайти(ЭлементМассива.TRANSACTION_ID,",");
				ПозицияДокумента = Сред(ЭлементМассива.TRANSACTION_ID,ПозицияРазделителя+1);
				ПозицияДокумента = Число(ПозицияДокумента);
				СтрокаДокумента = ДокументСсылка.ВыплатаЗаработнойПлаты[ПозицияДокумента-1];
				Банк = ПолучитьБанкИзСсылкиНаДокумент(ДокументСсылка,ПредопределенноеЗначение("Справочник.Банки.ПустаяСсылка")); // Переделать
				СтатусСсылка = ПолучитьСтатусПоЧисловомуЗначению(Статус); //ПредопределенноеЗначение("Перечисление.СтатусыДокументовВИнтернетБанке.T");
				
				ИнтеграцияСБанком_НаСервере.ВнестиНовыйСтатусДокумента(ДокументСсылка,СтатусСсылка,СтрокаДокумента.Сотрудник,ЛОЖЬ,ID_Отправки,Банк,ПозицияДокумента);
				
			КонецЦикла;
			
		Иначе
			ТаблицаПозиций = ПолучитьИзВременногоХранилища(ТаблицаПозицийАдресВременногоХранилища);
			
			МассивДокументов = Новый Массив;      
			Если СтруктураОтвета.Свойство("Data") Тогда    
				СтруктураData = СтруктураОтвета.Data;
				Если СтруктураData.Свойство("TransfersStatus") Тогда
					МассивДокументов = СтруктураData.TransfersStatus; 
				КонецЕсли;
			КонецЕсли;   
			
			Для Каждого ЭлементМассива Из МассивДокументов Цикл 
				
				Статус = ЭлементМассива.TransferStatus;
				ПозицияДокумента = ЭлементМассива.REC_ID;   
				Банк = ПолучитьБанкИзСсылкиНаДокумент(ДокументСсылка,ПредопределенноеЗначение("Справочник.Банки.ПустаяСсылка")); // Переделать
				СтатусСсылка = ПолучитьСтатусПоЧисловомуЗначению(Статус); //ПредопределенноеЗначение("Перечисление.СтатусыДокументовВИнтернетБанке.T");
				
				ИнтеграцияСБанком_НаСервере.ВнестиНовыйСтатусДокумента(ДокументСсылка,СтатусСсылка,,ЛОЖЬ,ID_Отправки,Банк,ПозицияДокумента);
				
			КонецЦикла;
			
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры         

Функция ПолучитьСтатусПоЧисловомуЗначению(НомерСтатуса)
	
	СтатусВозврата = Неопределено;
	
	Если НомерСтатуса = 0 Тогда 
		СтатусВозврата = ПредопределенноеЗначение("Перечисление.СтатусыДокументовВИнтернетБанке.G");	
	ИначеЕсли НомерСтатуса = 1 Тогда
		СтатусВозврата = ПредопределенноеЗначение("Перечисление.СтатусыДокументовВИнтернетБанке.Saved");
	ИначеЕсли НомерСтатуса = 2 Тогда
		СтатусВозврата = ПредопределенноеЗначение("Перечисление.СтатусыДокументовВИнтернетБанке.Authorized");
	ИначеЕсли НомерСтатуса = 3 Тогда
		СтатусВозврата = ПредопределенноеЗначение("Перечисление.СтатусыДокументовВИнтернетБанке.Finished");	
	ИначеЕсли НомерСтатуса = 4 Тогда
		СтатусВозврата = ПредопределенноеЗначение("Перечисление.СтатусыДокументовВИнтернетБанке.Rejected");	
	Иначе
		СтатусВозврата = ПредопределенноеЗначение("Перечисление.СтатусыДокументовВИнтернетБанке.ПустаяСсылка");
	КонецЕсли;
	
	Возврат СтатусВозврата;	
	
КонецФункции

Процедура ОбновитьСтатусДокумента(ДокументСсылка,СтруктураОтвета,СтруктураПарметровЗапросаВБанк,ID_Отправки) ЭКСПОРТ
	
	Если СтруктураОтвета.Свойство("ТекстОшибки") Тогда 
		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(СтруктураОтвета.ТекстОшибки);
		
	Иначе
		
		Если ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.РасходСоСчета")
			И (ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.Зарплата 
			ИЛИ ДокументСсылка.ВидОперации = Перечисления.ВидыОперацийРасходСоСчета.ЗарплатаВРазрезеСотрудников) Тогда
			
			МассивДокументов = Новый Массив;      
			Если СтруктураОтвета.Свойство("Data") Тогда    
				СтруктураData = СтруктураОтвета.Data;
				Если СтруктураData.Свойство("TransfersStatus") Тогда
					МассивДокументов = СтруктураData.TransfersStatus; 
				КонецЕсли;
			КонецЕсли;   
			
			Для Каждого ЭлементМассива Из МассивДокументов Цикл 
				
				Статус = ЭлементМассива.TransferStatus;
				УИДДокумента = ЭлементМассива.REC_ID; 
				ПозицияРазделителя = СтрНайти(ЭлементМассива.TRANSACTION_ID,",");
				ПозицияДокумента = Сред(ЭлементМассива.TRANSACTION_ID,ПозицияРазделителя+1);
				ПозицияДокумента = Число(ПозицияДокумента);
				СтрокаДокумента = ДокументСсылка.ВыплатаЗаработнойПлаты[ПозицияДокумента-1];
				Банк = ПолучитьБанкИзСсылкиНаДокумент(ДокументСсылка,ПредопределенноеЗначение("Справочник.Банки.ПустаяСсылка")); // Переделать
				СтатусСсылка = ПолучитьСтатусПоЧисловомуЗначению(Статус); //ПредопределенноеЗначение("Перечисление.СтатусыДокументовВИнтернетБанке.T");
				
				ИнтеграцияСБанком_НаСервере.ВнестиНовыйСтатусДокумента(ДокументСсылка,СтатусСсылка,СтрокаДокумента.Сотрудник,ЛОЖЬ,ID_Отправки,Банк,ПозицияДокумента);
			КонецЦикла;
			
		Иначе
			
			МассивДокументов = Новый Массив;      
			Если СтруктураОтвета.Свойство("Data") Тогда    
				СтруктураData = СтруктураОтвета.Data;
				Если СтруктураData.Свойство("TransfersStatus") Тогда
					МассивДокументов = СтруктураData.TransfersStatus; 
				КонецЕсли;
			КонецЕсли;   
			
			Для Каждого ЭлементМассива Из МассивДокументов Цикл 
				
				Статус = ЭлементМассива.TransferStatus;
				ПозицияДокумента = ЭлементМассива.REC_ID;   
				Банк = ПолучитьБанкИзСсылкиНаДокумент(ДокументСсылка,ПредопределенноеЗначение("Справочник.Банки.ПустаяСсылка")); // Переделать
				СтатусСсылка = ПолучитьСтатусПоЧисловомуЗначению(Статус); //ПредопределенноеЗначение("Перечисление.СтатусыДокументовВИнтернетБанке.T");
				
				ИнтеграцияСБанком_НаСервере.ВнестиНовыйСтатусДокумента(ДокументСсылка,СтатусСсылка,,ЛОЖЬ,ID_Отправки,Банк,ПозицияДокумента);
				
			КонецЦикла; 
			
		КонецЕсли;
		
	КонецЕсли;

КонецПроцедуры

Функция ПроверитНаМеждународнуюОтправкуСчетПолучателя(БанковскийСчетСсылка)
	
	Если Прав(БанковскийСчетСсылка.Банк.СВИФТБИК, 4) = "GE22" Тогда
		
		//Если ЗначениеЗаполнено(БанковскийСчетСсылка.Банк.Страна) И БанковскийСчетСсылка.Банк.Страна = Справочники.СтраныМира.Грузия Тогда 
		//    Возврат ЛОЖЬ;      
		//ИначеЕсли НЕ ЗначениеЗаполнено(БанковскийСчетСсылка.Банк.Страна) Тогда    
		//	// Исходим что страна може быть не заполнина у банков с внутреней транзакцией
		//	Возврат ЛОЖЬ;
		//Иначе
		//	Возврат ИСТИНА;	   
		//КонецЕсли;
		
	Иначе 
		
		Возврат ИСТИНА;
		
	КонецЕсли;    
	
	Возврат ЛОЖЬ;

КонецФункции // ПроверитНаМеждународнуюОтправкуСчетПолучателя(БанковскийСчетСсылка)
