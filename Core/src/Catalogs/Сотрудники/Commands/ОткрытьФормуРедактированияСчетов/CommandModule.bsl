
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	СтруктураПараметров = ПолучитьСтруктуруПараметров(ПараметрКоманды);
	ОткрытьФорму(
		"Справочник.Сотрудники.Форма.ФормаРедактированияСчетовУчета",
		СтруктураПараметров,
		ПараметрыВыполненияКоманды.Источник,
		ПараметрыВыполненияКоманды.Уникальность,
		ПараметрыВыполненияКоманды.Окно,
		ПараметрыВыполненияКоманды.НавигационнаяСсылка
	);
	
КонецПроцедуры

&НаСервере
Функция ПолучитьСтруктуруПараметров(ПараметрКоманды)
	
	СтруктураПараметров = Новый Структура(
		"СчетРасчетовСПерсоналом, СчетРасчетовСПодотчетниками, СчетРасчетовПоПерерасходу, Ссылка, СчетРасчетовСПерсоналомРегл, СчетРасчетовСПодотчетникамиРегл, СчетРасчетовПоПерерасходуРегл",
		ПараметрКоманды.СчетРасчетовСПерсоналом, ПараметрКоманды.СчетРасчетовСПодотчетниками, ПараметрКоманды.СчетРасчетовПоПерерасходу, ПараметрКоманды.Ссылка, ПараметрКоманды.СчетРасчетовСПерсоналомРегл, ПараметрКоманды.СчетРасчетовСПодотчетникамиРегл, ПараметрКоманды.СчетРасчетовПоПерерасходуРегл);
		
	Возврат СтруктураПараметров;
	
КонецФункции
