
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Компания = УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеНастройки("ОсновнаяОрганизация");
	ВозможностьДобавлятьНовыеВидыОтпусков = РегистрыСведений.УчетнаяПолитикаРегл.ЗначениеПоляУчетнойПолитики("ВозможностьДобавлятьНовыеВидыОтпусков", Компания);
	
	УстановитьВидимость();
	
КонецПроцедуры

&НаСервере
Процедура УстановитьВидимость()

	Элементы.ВидВариантовВидовОтпуска.Видимость =  ВозможностьДобавлятьНовыеВидыОтпусков;

КонецПроцедуры

&НаКлиенте
Процедура СписокПередНачаломДобавления(Элемент, Отказ, Копирование, Родитель, Группа, Параметр)
	
	Если НЕ ВозможностьДобавлятьНовыеВидыОтпусков Тогда 
		Отказ = Истина;
		ПоказатьПредупреждение(,"В учетной политике для компании: " + Компания + " не разрешено создание новых видов отпусков!");
		Возврат;
	КонецЕсли;
	
КонецПроцедуры
